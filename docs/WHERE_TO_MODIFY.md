# Where to Modify X

Quick lookup: find the feature you want to change, get every file it touches. For deeper detail on any file, jump to its card in [FILE_REFERENCE.md](FILE_REFERENCE.md).

| Feature | Route(s) | Data access | Templates | Static | Table(s) |
|---|---|---|---|---|---|
| **Login / Register / Forgot Password** | `routes/auth.py` | Supabase (PostgreSQL) via `database/supabase_client.py` on `admins` — as of 2026-07-23 (ADR-16); was raw SQLite until this cutover. `register()` also mirror-inserts into SQLite (`database/db.py`) so 7 tables' SQLite FKs to `admins.admin_id` keep resolving for new admins — `enquiries`, `students`, `audit_log`, `library_settings`, `membership_settings`, `backup_log`, `security_settings` | `templates/auth/*.html` (extends `auth_base.html`) | `css/login.css`, `js/login.js` | `admins` (Supabase is authoritative for login; SQLite copy kept in sync for row *existence* only. `password` also has a single writer again as of 2026-07-23 — see TD-35 `Resolved`: `routes/setting.py`'s password-change form now writes the same Supabase copy, ADR-17) |
| **Dashboard (KPIs, quick actions)** | `routes/dashboard.py` | raw SQL + `utils/charts.py` (`generate_revenue_chart`, `generate_membership_chart`) + `database/cashbook_queries.py` (`get_pending_fees`, `get_total_fee_revenue`) | `templates/dashboard/index.html` + `components/{dashboard_header,quick_actions,revenue_chart,membership_chart,expiry_table,recent_admissions,*_modal}.html` | `js/dashboard-charts.js`, `js/transaction_modal.js` | `students`, `memberships`, `payments`, `enquiries` |
| **Enquiries** | `routes/enquiries.py` | Supabase (PostgreSQL) via `database/supabase_client.py` on `enquiries` — as of 2026-07-23 (ADR-18); was raw SQLite until this cutover. `add()`/`edit()`/`delete()` also mirror-write into SQLite (`database/db.py`) so `routes/student.py`'s `admission()` keeps working against a real SQLite FK | `templates/enquiries/*.html` | — | `enquiries` (Supabase is authoritative for the Enquiries pages; SQLite copy kept in sync for `full_name`/`mobile`/`purpose`/`preferred_shift`/`followup_date`/`remarks`. `status` now also has a single writer again as of 2026-07-23 — see TD-36 `Resolved`: `routes/student.py`'s `admission()` flips `status` in Supabase directly, ADR-19) |
| **Students / Admissions** | `routes/student.py` | Supabase (PostgreSQL) via `database/supabase_client.py` on `students` — as of 2026-07-23 (ADR-19); was raw SQLite until this cutover. `admission()`/`edit()` also mirror-write into SQLite (`database/db.py`) so `routes/membership.py`/`routes/payment.py`/`routes/dashboard.py`/`routes/membership_distribution.py`/`routes/notification.py`/`routes/setting.py`'s backup functions/`database/bi_queries.py`/`cashbook_queries.py`/`membership_queries.py` (all unmigrated) keep working against a real SQLite FK and their own raw `JOIN students` queries | `templates/students/*.html` | — | `students` (Supabase is authoritative for the Students pages; SQLite copy kept in sync for every column), `enquiries` (`admission()` reads/writes this via Supabase directly too, same copy `routes/enquiries.py` owns — closes TD-36), `memberships`, `payments` (still SQLite-only, read directly by `index()`/`view()`) |
| **Memberships (create/renew)** | `routes/membership.py` | Supabase (PostgreSQL) via `database/supabase_client.py` on `memberships` — as of 2026-07-23 (ADR-20); was raw SQLite until this cutover. `create()`/`renew()` also mirror-write into SQLite (`database/db.py`) so `routes/payment.py`'s `collect()`/`routes/dashboard.py`/`routes/membership_distribution.py`/`routes/notification.py` (all unmigrated) keep working against their own raw `JOIN memberships` queries; also uses `database/payment_queries.py` (`record_payment`, SQLite-only), `database/membership_settings_queries.py` (`get_membership_settings`), and `database/membership_queries.py` (`get_active_membership` — now Supabase-backed, ADR-20 — plus `get_effective_status`, `get_plan_pricing`, `get_admission_fee`) | `templates/memberships/{index,create,renew}.html` | — | `memberships` (Supabase is authoritative for `index()`/`create()`/`renew()`; SQLite copy kept in sync for every column, since `routes/payment.py`'s `collect()` still updates `paid_amount`/`pending_amount` in SQLite only — see TD-37), `payments`, `cashbook`, `audit_log` (all still SQLite-only) — **note:** `create()` now redirects to `renew()` instead of creating a second membership if one is already active (2026-07-21); plan duration/fee auto-fill from `membership_settings` (TD-7 partially resolved); as of 2026-07-22, both routes reject negative `paid_amount`/`due_amount` and roll back on any DB error mid-write (TD-22 resolved) |
| **Membership Distribution (plan analytics)** | `routes/membership_distribution.py` | raw SQL + `utils/charts.py` (`generate_membership_distribution_donut`) + `database/cashbook_queries.py` (`get_pending_fees`, `get_total_fee_revenue`) + `database/membership_queries.py` (`get_membership_counts`, `get_effective_status`) | `templates/memberships/distribution.html` + `components/membership_*.html` | `css/membership_distribution.css`, `js/membership_distribution.js` | `memberships`, `students`, `payments` |
| **Membership effective status / days-to-expiry (single source of truth)** | n/a — pure data-access, called from Memberships, Dashboard, Student profile, Membership Distribution, Notifications | `database/membership_queries.py` (`EFFECTIVE_STATUS_SQL`, `get_effective_status`, `DAYS_LEFT_SQL`, `get_membership_counts`, `get_active_membership`) | — | — | `memberships` — **note:** `membership_status` never auto-flips to `'Expired'` in the DB; this module is the one place that combines it with `end_date` to get the real status (ADR-12, resolves TD-6). Changing this changes what "Active"/"Expired" means everywhere at once — don't reintroduce a local copy of the date comparison |
| **Fee revenue / pending fees (single source of truth)** | n/a — pure data-access, called from Dashboard, Membership Distribution, Cashbook and Business Intelligence | `database/cashbook_queries.py` (`get_total_fee_revenue`, `get_pending_fees`) | — | — | `payments`, `memberships` — **note:** deliberately distinct from `get_total_income()` (all Cashbook income, including non-fee categories); see ADR-11 in [DECISIONS.md](DECISIONS.md). Changing either function changes Dashboard's "Total Revenue"/"Pending Fees", Membership Distribution's "Revenue Collected"/"Pending Payments", Cashbook's "Pending Fees", and the BI health score's collection component all at once — that's the point, don't reintroduce a second copy in any one of them |
| **Membership Analytics** *(redirect shim — fixed 2026-07-22, see PF-2)* | `routes/membership_analytics.py` | none | none — redirects to `membership_distribution.index` | — | — |
| **Payments (collect fee)** | `routes/payment.py` | raw SQL + `database/payment_queries.py` (`record_payment`) | `templates/payments/{index,collect}.html` | — | `payments`, `memberships`, `cashbook`, `audit_log` — **note:** as of 2026-07-22, `collect()` refuses to run against a membership with no pending balance and rolls back on any DB error mid-write (TD-22 resolved) |
| **Cashbook (ledger, manual entries, audit log)** | `routes/cashbook.py` | `database/cashbook_queries.py`, `database/audit_queries.py`, `database/cashbook_categories.py` | `templates/cashbook/index.html` + `components/cashbook_*.html` | `css/cashbook.css`, `js/cashbook.js`, `js/transaction_modal.js` | `cashbook`, `audit_log` |
| **Business Intelligence (health score, growth, action items)** | `routes/business_intelligence.py` | `database/bi_queries.py`, `database/cashbook_queries.py` | `templates/business_intelligence/index.html` + `components/bi_*.html` | `css/business_intelligence.css`, `js/business_intelligence.js` | `cashbook`, `memberships`, `students` |
| **Notifications (expiry buckets + navbar bell)** | `routes/notification.py` (`index()` for the page, `get_notification_summary()` for the navbar bell via `app.py`'s context processor) | raw SQL + `database/membership_queries.py` (`DAYS_LEFT_SQL`) | `templates/notification/index.html`, `components/notification_dropdown.html` | — | `memberships`, `students` — **note:** bucket window (today/tomorrow/3-day/expired) is hardcoded and does not read Notification Settings' `reminder_*`/`notify_*` toggles yet (TD-28) |
| **Settings → Library Profile** | `routes/setting.py` (`library_profile`, `remove_library_logo`) | `database/settings_queries.py` | `templates/settings/library_profile.html` | `css/settings.css`, `js/settings.js` | `library_settings` |
| **Settings → Membership Settings (plan pricing/policy)** | `routes/setting.py` (`membership_settings`) | `database/membership_settings_queries.py`, `database/notification_settings_queries.py` (read-only, for the Reminders & Notifications summary) | `templates/settings/membership_settings.html` | — | `membership_settings` — **note:** plan duration/fee/admission-fee now read by `routes/membership.py`'s create/renew flow as of 2026-07-21 via `database/membership_queries.py` (TD-7 partially resolved); `late_fee_per_day`/`renewal_grace_days`/`auto_expiry`/`allow_early_renewal` still unread anywhere. `reminder_days`/`send_reminders` no longer written here (TD-23) — see Notification Settings row below |
| **Settings → Receipt Settings (numbering/branding/printing)** | `routes/setting.py` (`receipt_settings`) | `database/receipt_settings_queries.py` | `templates/settings/receipt_settings.html` | `css/settings.css`, `js/settings.js` | `library_settings` — **note:** `receipt_prefix`/`next_receipt_number` are now actually consumed by `database/payment_queries.py`'s `generate_receipt_number()` as of 2026-07-22 (TD-22 resolved, ADR-13). `auto_increment_receipt` is still inert — the counter always advances regardless, since no manual-receipt-number entry field exists to make "off" meaningful yet. `paper_size`/`auto_print`/`auto_email`/`open_pdf_after_save`/`duplicate_copy`/`print_logo`/`print_stamp`/`print_signature` remain unread (no receipt-printing/PDF/email code exists at all) |
| **Notification/reminder behavior (reminder days, channels, quiet hours, dashboard-display toggles)** | `routes/setting.py` (`notification_settings`); also read by `routes/dashboard.py` (`dashboard`, for `dash_show_pending_fees`) and `app.py` (`inject_notification_summary`, for the navbar bell's `nav_notification_prefs`) | `database/notification_settings_queries.py` | `templates/settings/notification_settings.html`, `templates/settings/membership_settings.html` (read-only summary), `templates/components/notification_dropdown.html` (badge/category visibility), `templates/dashboard/index.html` (Pending Fees card) | `css/settings.css`, `js/settings.js` (quiet-hours enable/disable) | `library_settings` — **note:** single owner of this behavior as of 2026-07-21 (ADR-8); reminder-rule/channel/quiet-hours toggles are save-only, nothing dispatches SMS/Email/WhatsApp yet (TD-24); `dash_show_new_admissions` has no widget yet (TD-25) |
| **Settings → Staff & User Access** *(placeholder — PF-4)* | `routes/setting.py` (`staff_access`) | none | `templates/settings/staff_access.html` | `css/settings.css` (`.settings-coming-soon-*`, `.settings-role-card`) | — |
| **Backup export logic (Data & Backup)** | `routes/setting.py` (`data_backup`, `backup_export_csv`, `backup_create`) | `database/backup_queries.py` | `templates/settings/data_backup.html` | `css/settings.css` | `backup_log` — **note:** manual export/backup only, no scheduled backups yet (PF-5) |
| **Security/session preferences (Security Settings)** | `routes/setting.py` (`security_settings`) — password-change branch touches `admins` via `database/supabase_client.py` (Supabase, as of 2026-07-23, ADR-17 — was raw SQLite until this cutover) | `database/security_settings_queries.py` (session preferences only) | `templates/settings/security_settings.html` | `css/settings.css`, `js/settings.js` (password match check) | `security_settings` — **note:** password change is fully enforced; `session_timeout_minutes`/`remember_me_enabled`/`login_notifications_enabled` are persisted but **not enforced** anywhere yet (TD-26). Writes Supabase's `admins.password`, same copy `routes/auth.py`'s login reads (TD-35 `Resolved`) — a password changed here now takes effect on the next login. |
| **Reports** | `routes/report.py` — permanent redirect shim | — | `templates/reports/index.html` exists but is dead | — | — |
| **Receipt numbering (actual generation)** *(centralized 2026-07-22, see ADR-13)* | n/a — pure data-access, called from `routes/payment.py` (`collect`) and `routes/membership.py` (`create`/`renew`) | `database/payment_queries.py` (`generate_receipt_number`, `record_payment`) — reads `receipt_prefix`/`next_receipt_number` from `library_settings` and advances the counter in the same transaction as the payment | — | — | `payments.receipt_number`, `library_settings.next_receipt_number` — **note:** one implementation, not three; changing the format changes it everywhere at once, don't reintroduce a per-route inline formula |
| **Auth/session guard pattern** | Copy-pasted `if "admin_id" not in session: return redirect("/")` at the top of nearly every route function — no decorator or `before_request` hook exists yet | — | — | — | — |
| **Colors / design tokens** | — | — | — | `css/style.css` (`.sidebar-wrapper` vars), `css/business_intelligence.css` (`--bi-*`), `css/membership_distribution.css` (`--md-*`) — each page redefines its own tokens, not a single shared file | — | — |
| **Database schema changes** | — | `database/schema.sql` (fresh installs) **+** a new `database/migrate_*.py` script (existing installs) — there's no migration runner, both must be updated together | — | — | update [04_DATABASE_SCHEMA.md](04_DATABASE_SCHEMA.md) in the same change |

## If your change doesn't fit neatly into one row above

- Check [FILE_REFERENCE.md](FILE_REFERENCE.md) for the specific file's full dependency card (what it depends on, what depends on it) before editing.
- Check [09_DEPENDENCY_MAP.md](09_DEPENDENCY_MAP.md) for cross-blueprint `url_for` couplings that won't show up as Python imports.
- Check [11_FUTURE_WORK.md](11_FUTURE_WORK.md) — your change might already be a known, tracked issue.
