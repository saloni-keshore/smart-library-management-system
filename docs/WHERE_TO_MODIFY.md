# Where to Modify X

Quick lookup: find the feature you want to change, get every file it touches. For deeper detail on any file, jump to its card in [FILE_REFERENCE.md](FILE_REFERENCE.md).

| Feature | Route(s) | Data access | Templates | Static | Table(s) |
|---|---|---|---|---|---|
| **Login / Register / Forgot Password** | `routes/auth.py` | Supabase (PostgreSQL) via `database/supabase_client.py` on `admins` — as of 2026-07-23 (ADR-16); was raw SQLite until this cutover. `register()` used to also mirror-insert into SQLite (`database/db.py`) so other tables' SQLite FKs to `admins.admin_id` kept resolving for new admins; as of 2026-07-25 (ADR-31) that mirror-insert is deleted outright — Supabase is now the only store, and this file has zero SQLite dependency | `templates/auth/*.html` (extends `auth_base.html`) | `css/login.css`, `js/login.js` | `admins` (Supabase-only as of ADR-31. `password` also has a single writer again as of 2026-07-23 — see TD-35 `Resolved`: `routes/setting.py`'s password-change form writes the same Supabase copy, ADR-17) |
| **Dashboard (KPIs, quick actions)** | `routes/dashboard.py` | Supabase (PostgreSQL) via `database/membership_queries.py` (`get_membership_counts`, `get_memberships_for_admin`, `get_admin_students`, `get_days_left`) for students/enquiries/memberships — as of 2026-07-23 (ADR-23). Charts via `utils/chart_data.py` (`build_revenue_chart_data`, `build_membership_chart_data` — client-side Chart.js as of 2026-08-28, ADR-56, replacing `utils/charts.py`); `database/cashbook_queries.py`'s `get_total_fee_revenue`/`get_today_fee_collection`/`get_pending_fees` also Supabase (ADR-25) — this route has no SQLite dependency left at all | `templates/dashboard/index.html` (loads Chart.js CDN) + `components/{dashboard_header,quick_actions,revenue_chart,membership_chart,expiry_table,recent_admissions,*_modal}.html` | `js/dashboard-charts.js`, `js/transaction_modal.js` | `students`, `memberships`, `enquiries`, `payments` (all Supabase, ADR-23/25) |
| **Revenue Overview period switch (This Year/Last Year)** | `routes/dashboard.py`'s `revenue_chart()` (`GET /dashboard/revenue-chart?period=`) — returns a Chart.js `{labels, datasets}` object (ADR-56; was `{"image_url": ...}` before) | `utils/chart_data.py`'s `build_revenue_chart_data(admin_id, period)` / `_monthly_revenue_for_year(payments, year)`, via `database/payment_queries.get_payments_for_admin` | `components/revenue_chart.html` (`#revenue-period-select`, `#revenue-chart-canvas`, inline `window.dashboardRevenueChart`) | `js/dashboard-charts.js` — `fetch()`s the endpoint on `<select>` `change`, then `chart.data = json; chart.update()` | `payments` (Supabase) — to add a third period, add a value to the `<select>`, extend `revenue_chart()`'s allowed-`period` set, and give `build_revenue_chart_data()` a matching branch; a true "This Month" mode would also need a different (daily, not monthly) x-axis shape |
| **Enquiries** | `routes/enquiries.py` + `database/id_sequence.py` (`enquiry_id` assignment) | Supabase (PostgreSQL) via `database/supabase_client.py` on `enquiries` — as of 2026-07-23 (ADR-18), and as of 2026-07-24 (ADR-30) the only store, no SQLite mirror left | `templates/enquiries/*.html` | — | `enquiries` (Supabase-only as of ADR-30. `status` has a single writer — TD-36 `Resolved`: `routes/student.py`'s `admission()` flips it in Supabase directly. **As of 2026-08-28 (TD-78 + ADR-58):** a phone number identifies one person per admin — `add()` calls `_find_person_by_mobile()` and, if the number's on file, creates nothing and redirects to that person's `enquiry.view` / `student.view`. Repeat visits use the `re_enquire()` route ("Log Another Enquiry" on the enquiry + student detail pages), which updates the person's one enquiry row in place. `edit()` blocks a mobile change onto another person's number. `enquiry_id` assigned via `database/id_sequence.py`'s `insert_with_next_id()` (retries `MAX+1` on a primary-key collision). **As of 2026-08-30 (ADR-59):** `add()`/`edit()` canonicalize `mobile` via `utils/normalization.py`'s `clean_mobile()` — strips `+91`/spaces/dashes, requires a bare 10-digit result, else re-shows the form with an error) |
| **Students / Admissions** | `routes/student.py` + `database/id_sequence.py` (`student_id` assignment) | Supabase (PostgreSQL) via `database/supabase_client.py` on `students` — as of 2026-07-23 (ADR-19), and as of 2026-07-24 (ADR-29) the only store, no SQLite mirror left; `view()`'s `memberships`/`payments` reads also Supabase as of 2026-07-24 (ADR-25) | `templates/students/*.html` | — | `students` (Supabase-only as of ADR-29), `enquiries` (`admission()` reads/writes this via Supabase directly, same copy `routes/enquiries.py` owns — closes TD-36), `memberships`, `payments` (all Supabase-only, ADR-25/28/29) — **note (2026-08-28, TD-78):** `admission()` assigns `student_id` via `database/id_sequence.py`'s `insert_with_next_id()` (retries the `MAX+1` on a primary-key collision from a concurrent admission). **note (2026-08-30, ADR-59):** `edit()` validates `mobile` via `clean_mobile()` (bare 10 digits or error); `admission()` runs the inherited enquiry mobile through it too and blocks conversion (→ `enquiry.edit`) if it's malformed. **note (2026-08-30, ADR-60):** there's no delete-student — `edit()`'s `status` → `Inactive` is the "remove" path; it also mirrors status onto the linked enquiry (`Inactive`→`Inactive`, anything else incl. `Pending`→`Admitted`), and `templates/students/index.html`'s Status column reads `student.status` before membership state. Inactive is not yet filtered out of the list or BI counts — TD-80. **note (2026-08-30, ADR-61):** `admission()` inserts `status = "Pending"` (not `"Active"`) and requires `address`/`join_date` server-side; a student becomes `"Active"` only via `database/membership_queries.py`'s `promote_student_if_fully_paid()`, called from `membership.create()` and `payment.collect()` once a membership exists with zero pending balance. The Students list badges `Pending` amber, offers a "Complete Admission" action, and shows an "incomplete admissions" banner (`incomplete_count`). `Pending` conflates "abandoned" and "part-paid" — TD-81; legacy `Active`-but-incomplete rows are reported, not migrated — TD-82 |
| **Memberships (create/renew)** | `routes/membership.py` | Supabase (PostgreSQL) via `database/supabase_client.py` on `memberships` — as of 2026-07-23 (ADR-20), and as of 2026-07-24 (ADR-29) the only store, no SQLite mirror left; also uses `database/payment_queries.py` (`record_payment` — Supabase-only and strict, ADR-28), `database/membership_settings_queries.py` (`get_membership_settings`), and `database/membership_queries.py` (`get_active_membership` — now Supabase-backed, ADR-20 — plus `get_effective_status`, `get_plan_pricing`, `get_admission_fee`, and as of 2026-08-17 `insert_membership`/`DiscountColumnsUnavailable`, ADR-46) | `templates/memberships/{index,create,renew}.html` | — | `memberships` (Supabase is authoritative for `index()`/`create()`/`renew()`, and, as of 2026-07-23 (ADR-21), also for `routes/payment.py`'s `collect()` — Supabase-only as of ADR-29, no SQLite mirror left; TD-37 `Resolved`), `payments`, `cashbook`, `audit_log` (all Supabase-only, ADR-22/25/27/28) — **note:** `create()` now redirects to `renew()` instead of creating a second membership if one is already active (2026-07-21); as of 2026-07-22, both routes roll back on any DB error mid-write (TD-22 resolved); as of 2026-08-17 (ADR-45), `total_fee` for a standard plan is computed server-side from `membership_settings` (`get_plan_pricing`/`get_admission_fee`) — not client-editable auto-fill anymore — with `"Custom"` the only plan accepting a manually-typed `total_fee`; `pending_amount` is always derived as `total_fee - paid_amount`, and `paid_amount > total_fee` is rejected (TD-7 further narrowed, not fully closed — see its row in [11_FUTURE_WORK.md](11_FUTURE_WORK.md)); as of the same day (ADR-46), `create()` only additionally accepts a `discount_amount`/`discount_reason` (Total Payable minus Discount = the Final Payable stored as `total_fee`) — `memberships.discount_amount`/`discount_reason` need a manual `ALTER TABLE` on any pre-existing Supabase project (TD-58), and unlike other such gaps in this app, using the discount box before that's run hard-fails membership creation with a clear message rather than silently dropping the discount; as of 2026-08-21 (TD-30 fix, ADR-53), both `create()` and `renew()` check a posted `idempotency_key` hidden field via `database/membership_queries.py`'s `find_membership_by_idempotency_key()`/`insert_membership()` before doing any real work, so a double-click/back-button resubmit is deduplicated instead of creating a second membership — `memberships.idempotency_key` needs the same kind of manual `ALTER TABLE` as the discount columns (TD-66), but unlike discount, silently has no effect (not a hard failure) until then; as of 2026-08-29, `create.html`'s "Joining Date" field is pre-filled from `students.join_date` (the Join Date entered on admission) so it isn't asked twice — still editable, still a separate column, `renew.html` unaffected |
| **Membership Distribution (plan analytics)** | `routes/membership_distribution.py` | Supabase (PostgreSQL) via `database/membership_queries.py` (`get_membership_counts`, `get_memberships_for_admin`, `get_effective_status`) + `utils/chart_data.py` (`build_plan_distribution_chart_data` — client-side Chart.js doughnut as of 2026-08-28, ADR-56, replacing `utils/charts.py`'s `generate_membership_distribution_donut`) — as of 2026-07-23 (ADR-23). `receipt_number`/`payment_mode`/`payment_date`/`last_amount_paid` now come from `database/payment_queries.get_payments_for_admin()` (Supabase) as of 2026-07-24 (ADR-25) — this route has no SQLite dependency left at all. `get_total_fee_revenue`/`get_pending_fees` (`database/cashbook_queries.py`, both Supabase) | `templates/memberships/distribution.html` + `components/membership_*.html` | `css/membership_distribution.css`, `js/membership_distribution.js` | `memberships`, `students`, `payments` (all Supabase, ADR-23/25) |
| **Membership effective status / days-to-expiry (single source of truth)** | n/a — pure data-access, called from Memberships, Dashboard, Student profile, Membership Distribution, Notifications | `database/membership_queries.py` (`EFFECTIVE_STATUS_SQL`, `get_effective_status`, `DAYS_LEFT_SQL`, `get_membership_counts`, `get_active_membership`) | — | — | `memberships` — **note:** `membership_status` never auto-flips to `'Expired'` in the DB; this module is the one place that combines it with `end_date` to get the real status (ADR-12, resolves TD-6). Changing this changes what "Active"/"Expired" means everywhere at once — don't reintroduce a local copy of the date comparison |
| **Input normalization (Category/Name/Location/free-text/phone formatting, single source of truth)** *(added 2026-07-26, ADR-36)* | n/a — pure string functions, called from Enquiries, Students, Memberships, Payments, Library Profile | `utils/normalization.py` (`normalize_category` — Purpose/Shift/Membership Plan/Payment Mode → UPPERCASE; `normalize_name`/`normalize_location` — person names/City/State → Title Case; `normalize_free_text` — Address/Remarks/Receipt Footer, trim/collapse only; `normalize_phone` — trim/collapse only). One-time backfill for pre-existing rows: `database/migrate_normalize_input_data.py` (re-runnable, hand-run via `python -m database.migrate_normalize_input_data`) | `templates/students/edit.html` (Purpose/Shift `<select>` pre-selection compares against the UPPERCASE stored value) | — | `enquiries`, `students`, `memberships`, `payments`, `library_settings` — **note:** a new field that's a fixed-vocabulary category / person name / city-state / free text / phone number should call the matching function here, not a local `.upper()`/`.title()`/`.strip()`. `routes/membership_distribution.py`'s `plan_counts`/`utils/chart_data.py`'s `PLAN_CHART_COLORS` both read `memberships.plan_name` post-normalization — see their own notes above/below if you touch plan-name matching. Cashbook's own `category`/`payment_method` fields are deliberately **not** covered — see TD-48 in [11_FUTURE_WORK.md](11_FUTURE_WORK.md) before normalizing those |
| **Fee revenue / pending fees (single source of truth)** | n/a — pure data-access, called from Dashboard, Membership Distribution, Cashbook and Business Intelligence | `database/cashbook_queries.py` (`get_total_fee_revenue`/`get_today_fee_collection` — Supabase `students`/`payments` via a local `_fetch_payments_for_admin()`, as of 2026-07-24, ADR-25; `get_pending_fees` — Supabase `students`/`memberships` via `database.membership_queries.get_memberships_for_admin()`, as of 2026-07-23, ADR-23) | — | — | `payments`, `memberships`, `students` (all Supabase, ADR-23/25) — **note:** deliberately distinct from `get_total_income()` (all Cashbook income, including non-fee categories); see ADR-11 in [DECISIONS.md](DECISIONS.md). Changing either function changes Dashboard's "Total Revenue"/"Pending Fees", Membership Distribution's "Revenue Collected"/"Pending Payments", Cashbook's "Pending Fees", and the BI health score's collection component all at once — that's the point, don't reintroduce a second copy in any one of them |
| **Membership Analytics** *(redirect shim — fixed 2026-07-22, see PF-2)* | `routes/membership_analytics.py` | none | none — redirects to `membership_distribution.index` | — | — |
| **Payments (collect fee) / Receipt page** | `routes/payment.py` | Supabase (PostgreSQL) via `database/supabase_client.py` on `memberships` for `collect()`'s balance read/update — as of 2026-07-23 (ADR-21), and as of 2026-07-24 (ADR-29) the only store, no SQLite mirror left. `index()` reads Supabase `payments`/`students` via `database/payment_queries.get_payments_for_admin()` as of 2026-07-24 (ADR-25); `collect()`/`create()`/`renew()` use `database/payment_queries.py`'s `record_payment()` (Supabase-only and strict as of 2026-07-24, ADR-28) plus, as of 2026-07-25, `get_payment_id_by_receipt_number()` to find the new redirect target; the new `receipt(payment_id)` (added 2026-07-25) reads `payments`/`students`/`memberships` directly and this admin's `library_settings` via `database/receipt_settings_queries.get_receipt_settings()` | `templates/payments/{index,collect,receipt}.html` — `receipt.html` includes `components/receipt_document.html` (added 2026-07-25, ADR-33) | `css/settings.css` (`.receipt-preview-doc` rules, `receipt.html` only) | `memberships` (Supabase-only as of ADR-29, no SQLite mirror left), `payments` (Supabase-only as of ADR-28, no SQLite mirror left), `students` (Supabase-only as of ADR-29, no SQLite mirror left), `cashbook`, `audit_log` (Supabase-only, ADR-27/ADR-26) — **note:** as of 2026-07-22, `collect()` refuses to run against a membership with no pending balance and rolls back on any DB error mid-write (TD-22 resolved). As of 2026-07-25, `collect()`/`create()`/`renew()` redirect to `payment.receipt` (real receipt page) instead of `student.view` whenever a payment was actually recorded — see ADR-33. As of 2026-08-21 (TD-30/TD-43 fix, ADR-53), `collect()` checks a posted `idempotency_key` hidden field (`database/payment_queries.py`'s `find_payment_by_idempotency_key()`) **before** touching `memberships.paid_amount`/`pending_amount`, so a duplicate resubmission can't double-decrement the balance; `record_payment()` also now flags `payments.cashbook_synced = FALSE` if the automatic Cashbook entry fails even after its own retry (TD-43) — both `payments.idempotency_key`/`payments.cashbook_synced` need a manual `ALTER TABLE` on an already-provisioned project (TD-66), silently inert (not a hard failure) until then. **As of 2026-08-30 (ADR-63):** what the receipt shows for dates and the plan is computed in `receipt()` (`_fmt_receipt_date()`, `_plan_label()`) and passed as plain context vars — the receipt now has **Joining Date** + **Expiry Date** rows (from `memberships.joining_date`/`end_date`) and an issue date by the signature, and a Custom plan's Membership line reads `"Custom - N day(s)"` from `duration_days`. To change the receipt's visual layout, still edit only `components/receipt_document.html`; to change *which* value fills a row, edit `receipt()`. See TD-84 (`end_date` is browser-computed, not re-derived server-side) |
| **Admission Fee vs Membership Fee categorization** *(added 2026-08-30, ADR-62)* | `routes/membership.py` (`create()`, `renew()`), `routes/payment.py` (`collect()`) | `database/membership_queries.py`'s `split_admission_and_membership_fee(admission_fee_amount, old_paid, amount)` (the admission-fee-first waterfall, pure/stateless) and `AdmissionFeeColumnUnavailable` (hard-fail if a real, nonzero `admission_fee_amount` can't be persisted — same shape as `DiscountColumnsUnavailable`, ADR-46); `database/payment_queries.py`'s `record_payment()` now accepts `category` as a list of `(category, amount)` pairs, one Cashbook entry per pair | — (Revenue Sources chart itself is deliberately unchanged) | — | `memberships.admission_fee_amount` (new column, snapshotted once at `create()`, `0` for Custom/renewal — **requires a manual `ALTER TABLE` on an already-provisioned Supabase project, TD-83**, currently missing on this project's own database), `cashbook` (a real split now legitimately writes 2 rows for 1 `payment_id` — was already schema-safe, no `UNIQUE` on that column) — **note:** to change the split rule itself (e.g. proportional instead of admission-fee-first), edit only `split_admission_and_membership_fee()` — both `create()` and `collect()` already call it uniformly, so a rule change there applies everywhere without touching either route. `renew()` deliberately never calls it — a renewal is always tagged `"Membership Renewal"` outright (ADR-45's existing exclusion) |
| **Cashbook (ledger, manual entries, audit log)** | `routes/cashbook.py` | Supabase (PostgreSQL) via `database/supabase_client.py` on `cashbook`/`audit_log`, through `database/cashbook_queries.py`/`database/audit_queries.py` — as of 2026-07-23 (ADR-22), and as of 2026-07-24 (ADR-27) Supabase is the **only** store for `cashbook`, no SQLite mirror left at all. Manual entries (`insert_transaction`) write the `cashbook` row first and roll it back if the `audit_log` insert then fails; automatic entries (`insert_income_entry`, triggered by Membership/Payment) wrap ID generation and both inserts in one best-effort `try/except: pass` (see TD-43 — as of ADR-27 this has no SQLite fallback left, unlike TD-39/TD-41's staleness-only risk) — `payment_id` **is** sent to Supabase for those too since 2026-07-24 (ADR-25), now that `payments` itself has a Supabase row (closing TD-38's common case). Also `database/cashbook_categories.py` (constants) | `templates/cashbook/index.html` + `components/cashbook_*.html` | `css/cashbook.css`, `js/cashbook.js`, `js/transaction_modal.js` | `cashbook` (Supabase-only as of 2026-07-24, ADR-27 — the SQLite mirror-write was removed entirely, the second mirror actually deleted, not just migrated), `audit_log` (Supabase-only as of 2026-07-24, ADR-26 — the first mirror actually deleted; `cashbook_queries.py` writes the Supabase `audit_log` row directly). As of 2026-08-21 (TD-43 fix, ADR-53), `insert_income_entry()` retries once before giving up (was a single attempt), and `index()` shows a warning banner when `database/payment_queries.get_unsynced_payment_count(admin_id)` is nonzero (payments whose automatic ledger entry never made it) |
| **Income vs Expense period switch (This Year/Last Year)** *(added 2026-08-30)* | `routes/cashbook.py`'s `income_expense_chart()` (`GET /cashbook/income-expense-chart?period=`) — returns a Chart.js `{labels, datasets}` object from `_build_income_expense_chart(admin_id, year)`, same pattern as `routes/dashboard.py`'s `revenue_chart()` above; before this the `<select>` had no `id`/listener and the chart data had no year filter at all, so switching it did nothing | `_build_income_expense_chart(admin_id, year)` filters `database/cashbook_queries.py`'s `get_monthly_income`/`get_monthly_expense` (all-time `{"YYYY-MM": total}` dicts) down to the requested calendar year by string-prefix match | `components/cashbook_income_chart.html` (`#income-expense-period-select`, `#incomeExpenseChart`, inline `window.cashbookChartData`) | `js/cashbook.js` — `fetch()`s the endpoint on `<select>` `change`, then `chart.data = json; chart.update()` (keeps the `Chart` instance returned by `renderChart()` instead of discarding it) | `cashbook` (Supabase) — to add a third period, add a value to the `<select>`, extend `income_expense_chart()`'s allowed-`period` set, and give `_build_income_expense_chart()` a matching year/range |
| **Business Intelligence — Overview (health score, growth, action items)** | `routes/business_intelligence.py` (`index`) | `database/bi_queries.py` (Supabase, ADR-23, for its 3 membership-side functions; the rest reuses `database/cashbook_queries.py`, unchanged) | `templates/business_intelligence/index.html` + `components/bi_*.html` + `components/bi_components.html`'s `tabs()` macro (added 2026-07-25) | `css/business_intelligence.css`, `js/business_intelligence.js` | `cashbook`, `memberships`, `students` (Supabase, ADR-22/23) |
| **Business Intelligence — Purpose Analytics** *(real data as of 2026-07-26, Phase 1)* | `routes/business_intelligence.py` (`purpose_analytics`, builder `_purpose_analytics_data`) | `database/bi_queries.py` (`get_purpose_breakdown` — merges `database.membership_queries.get_admin_students`/`database.payment_queries.get_payments_for_admin` in Python) | `templates/business_intelligence/purpose_analytics.html`, built from `components/bi_components.html` macros — unchanged, consumes whatever `data` the route passes | `css/business_intelligence.css` (extended, same file Overview uses), `js/bi_purpose_analytics.js` — unchanged | `students` (`purpose`, `status`), `payments` (`amount_paid`, `student_id`) (Supabase) |
| **Business Intelligence — Revenue Analytics** *(real data as of 2026-07-26, Phase 2, ADR-37)* | `routes/business_intelligence.py` (`revenue_analytics`, builder `_revenue_analytics_data`) | `database/bi_queries.py` (`get_revenue_time_windows`, `get_monthly_fee_revenue`, `get_revenue_by_plan`, `get_revenue_by_payment_mode`, `get_payment_mode_usage_counts`, `get_new_vs_renewal_revenue`, `get_revenue_collection_summary`, `get_avg_revenue_per_student`, plus the existing `get_purpose_breakdown`) — merges `database.payment_queries.get_payments_for_admin`/`database.membership_queries.get_memberships_for_admin`/`get_admin_students` in Python | `templates/business_intelligence/revenue_analytics.html`, built from `components/bi_components.html` macros — gained a second KPI row and two new chart cards (`paymentModeChart`, `newVsRenewalChart`) plus a Business Insights row in this same change | `css/business_intelligence.css` (extended, same file Overview uses), `js/bi_revenue_analytics.js` (extended with two new `renderPie()` calls) | `payments` (`amount_paid`, `payment_date`, `payment_mode`, `membership_id`, `student_id`), `memberships` (`plan_name`, `total_fee`, `paid_amount`, `pending_amount`, `membership_id`, `student_id`) (Supabase) — **note:** deliberately Payments/Memberships-scoped, not Cashbook's broader `get_monthly_income()` total (ADR-11/ADR-37); New-vs-Renewal is classified by `membership_id` order per student, not Cashbook's best-effort `source` tag — see ADR-37 before changing either |
| **Business Intelligence — Occupancy Analytics** *(real data as of 2026-07-26, Phase 3, ADR-38)* | `routes/business_intelligence.py` (`occupancy_analytics`, builder `_occupancy_analytics_data`) | `database/bi_queries.py` (`get_shift_capacities`, `get_active_memberships`, `get_shift_occupancy`, `get_occupancy_summary`, `get_occupancy_by_purpose`, `get_occupancy_by_plan`, `get_purpose_shift_matrix`, `get_monthly_occupancy_trend`, `get_occupancy_insights`) — merges `database.membership_queries.get_memberships_for_admin`/`get_effective_status` with `database.settings_queries.get_library_settings` in Python | `templates/business_intelligence/occupancy_analytics.html`, built from `components/bi_components.html` macros plus `components/bi_occupancy_insights.html` (new) — gained a second KPI row, three new chart cards, an AI Insights section, two insight cards, and a second table (Purpose Shift Matrix) in this same change | `css/business_intelligence.css` (extended, same file Overview uses), `js/bi_occupancy_analytics.js` (extended with three new render functions) | `students` (`shift`, `purpose`), `memberships` (`membership_status`, `end_date`, `joining_date`, `plan_name`, `student_id`), `library_settings` (`morning_capacity`/`afternoon_capacity`/`evening_capacity`, new) — **note:** "occupied" means a currently effectively-active membership, not every student ever admitted (deliberately narrower than Purpose Analytics — see ADR-38); capacity is a genuinely configurable admin setting, not a fabricated number, but the live database may not have the `*_capacity` columns applied yet (see the Settings → Library Profile row below and ADR-38) |
| **AI Center — Student Risk Analysis** *(Phase 1, added 2026-07-27, ADR-39; autocomplete search, ADR-41)* | `routes/ai_center.py` (`index`, `student_suggestions`) | `database/ai_center_queries.py` (`compute_student_risk`, `search_students`, `get_student_detail`) — merges `database.membership_queries.get_admin_students`/`get_days_left`/`get_memberships_for_admin` with direct `memberships`/`payments` reads scoped by `student_id`; reads weights/thresholds via `database.ai_center_settings_queries.get_effective_settings` (as of ADR-40) | `templates/ai_center/student_risk.html`, `components/ai_center_components.html` (shared tab bar) | `css/ai_center.css`, `js/ai_center_search.js` | `students`, `memberships`, `payments` (Supabase) — **note:** risk score/reasons are rule-based (payment delay, renewal timing, plan length), not a trained model — see ADR-39. Weights/thresholds are no longer hardcoded — see the Settings row below. Search is a live autocomplete (case-insensitive, partial, on name or mobile) that only loads a student's risk breakdown on explicit selection — `index()` no longer accepts a `q` parameter at all (ADR-41); to change what a suggestion shows, edit `search_students()`'s enrichment, not the route |
| **AI Center — Settings** *(weights/thresholds, added 2026-07-27, ADR-40; contextual help + visual bars, ADR-42)* | `routes/ai_center.py` (`settings`, `_parse_settings_form`) | `database/ai_center_settings_queries.py` (`get_ai_center_settings`, `get_effective_settings`, `save_ai_center_settings`, `DEFAULTS`) | `templates/ai_center/settings.html`, `components/ai_center_components.html` (`tabs()` and `info_tip()` macros) | `css/ai_center.css`, `js/ai_center_settings.js` | `ai_center_settings` (Supabase, new table — **not yet applicable by this app itself, see TD-53**: until an admin's Supabase project owner runs the `CREATE TABLE` in `database/supabase_migration.sql` by hand, reads fall back to `DEFAULTS` and saves flash a clear error instead of persisting) — **note:** weights (payment delay / renewal history / membership duration) must sum to 100; `high_risk_max` must be less than `low_risk_min` — both validated in `_parse_settings_form()` before any write, not a DB constraint (see ADR-40). Each field's plain-language "what it does / increase / decrease" explanation lives in the `aic.info_tip(...)` call beside it in `settings.html`, not in `ai_center_settings.js` — the JS only opens/closes the popover and redraws the (cosmetic-only) risk-range/weight bars, it holds no copy |
| **Panda / Smart Business Assistant** *(floating widget, added 2026-07-27, ADR-43; Part 1 "Ask" added 2026-08-18, ADR-47; Part 2 "Analyze" added 2026-08-18, ADR-48; Part 3 "Explain" added 2026-08-19, ADR-49; Part 4 "Recommend" added 2026-08-19, ADR-50; Part 5 "Forecast" added 2026-08-19, ADR-51; retention/cash topic expansion added 2026-08-20, ADR-52; reliability + expenses/coverage pass added 2026-08-24, ADR-54; cash-balance/profit-loss topic expansion added 2026-08-25, ADR-55 — see `PANDA_SPEC.md`)* | `panda/routes.py` (`insights`, `suggested_questions`, `conversations`, `messages`) | `panda/services.py` → `panda/notifications.py`/`panda/insights.py` (wrap `database.bi_queries.get_revenue_growth`/`get_occupancy_summary`, `routes.notification.get_notification_summary` — no new query logic of their own) for insight cards; `send_message()` → `panda/intents.py`'s `generate_reply()` for chat replies — a rule/keyword-based classifier, no LLM/ML, answering revenue, "why is revenue X" (`get_revenue_explanation`, ranked real factors from admissions momentum/unrenewed expirations/pending collections — see TD-61 for what it can't say, category-level causes), occupancy (as of 2026-08-24, a forward-looking occupancy question like "will occupancy increase?" routes to forecast instead), renewals, admissions trend (`panda/insights.py`'s `get_admissions_trend`, wrapping `database.bi_queries.get_monthly_new_memberships` — as of 2026-08-24 the reply also states total roster size via `get_student_count` and names the best admission month), purpose/course performance (`get_purpose_performance`, wrapping `get_purpose_breakdown` — also answers "which batch should I promote?" via the same data, with an explicit "not a real batch" disclaimer), student risk (`get_top_risk_students`, as of 2026-08-24 calling `database.ai_center_queries.compute_student_risk_batch()` — a constant number of Supabase round trips, not a per-student loop, see TD-60/ADR-54 — over `database.membership_queries.get_admin_students`, capped at 100 students), "how can I increase profit?"/"what should I do?" (`get_recommended_actions`, a pure wrapper around `database.bi_queries.get_action_items` — Business Intelligence's own Action Center, no new recommendation logic), "what's expected next month?" (`get_revenue_forecast`/`get_seat_demand_forecast`/`get_churn_forecast`, wrapping `panda/forecasting.py`'s plain-Python linear trend extrapolation over real monthly history — never a fabricated number, `None` below `MIN_MONTHS_FOR_FORECAST`, see TD-62), "how can I retain students?"/"how do I reduce churn?" (`get_retention_summary`/`get_churn_forecast`/`get_top_risk_students` composed, plus `get_risk_scoring_coverage` to honestly surface TD-60's cap when hit — ADR-52; deliberately no cross-student "why" aggregate, see TD-65), "how can I manage cash?"/"where is my cash going?" (`get_cash_collection_summary`/`get_expense_breakdown`/`get_expense_health` composed, wrapping `database.bi_queries.get_revenue_collection_summary`/`get_top_expense_categories`/`classify_expense_health` — ADR-52, no new query; as of 2026-08-24 the expense-category line is explicitly labeled "all-time" to avoid reading as a this-month figure), a dedicated expenses question ("how much are my expenses?", "what are my biggest expenses?", ADR-54) — `get_expense_breakdown`/`get_expense_health` composed without the fee-collection section, reusing the same two wrappers cash-management already uses, and (added 2026-08-25, ADR-55) "how much cash do I have?"/"what's my cash balance?" (`get_cash_balance`, wrapping `database.cashbook_queries.get_cash_balance` — Cash-payment-method income minus Cash-payment-method expense, all-time; explicitly never called "bank balance"/"total money available"/"total collections") and "tell me about my profit or loss?"/"am I making a profit?" (`get_profit_loss_summary`, wrapping `database.cashbook_queries.get_total_income`/`get_total_expense`/`get_monthly_profit` — an explicit all-time net across every payment method plus a recent-months breakdown that marks the current month partial; kept as a deliberately separate reply from cash balance, since the two can carry opposite signs at once, see ADR-55) — all from real data; declines action requests (deletion/pricing-change wording); "which locality gives the highest admissions" deliberately stays unanswered — no locality/city data exists anywhere on `students`; falls through to `panda/prompts.py`'s `generate_placeholder_reply()` for anything else; `database/panda_queries.py` for chat persistence (as of 2026-08-24, distinguishes a missing-table failure, `ChatStorageUnavailable`, from a transient network failure, `ChatStorageTemporarilyUnavailable` — see ADR-54) | `templates/components/panda_widget.html` (included from `layouts/base.html` for logged-in sessions only) | `css/panda.css`, `js/panda.js` (as of 2026-08-24, its session-lapse-vs-backend-failure classification is evidence-based, not "any unparseable response = session expired" — see ADR-54) | `panda_conversations`, `panda_messages` (Supabase, new tables — **not yet applicable by this app itself, see TD-55**, same manual-creation shape as `ai_center_settings`/TD-53: until applied, chat endpoints 503 with a clear message; insights/suggested-questions are unaffected since they read no table of their own) — **note:** still no LLM or ML model exists (see ADR-47/48/49/50/51/52/54/55) — a new answerable intent means adding a keyword tuple + `_reply_*` function in `panda/intents.py`, not touching `prompts.py`/`services.py`. `panda/forecasting.py` is now real (Part 5) — its three functions are the intended swap point for a genuine ML/LLM model later (see PF-8). The retention/cash/expenses/cash-balance/profit-loss topic expansions (ADR-52/ADR-54/ADR-55) are a lateral extension of Part 1's keyword coverage, not a sixth part of the progression. `get_cash_balance()` has no monthly breakdown (all-time only, see TD-71) |
| **Notifications (expiry buckets + navbar bell)** | `routes/notification.py` (`index()` for the page, `get_notification_summary()` for the navbar bell via `app.py`'s context processor) | Supabase (PostgreSQL) via `database/membership_queries.py` (`get_memberships_for_admin`, `get_days_left`) — as of 2026-07-23 (ADR-23); was raw SQLite until this cutover. This route has no SQLite dependency left | `templates/notification/index.html`, `components/notification_dropdown.html` | — | `memberships`, `students` (Supabase, ADR-23) — **note:** bucket window (today/tomorrow/3-day/expired) is hardcoded and does not read Notification Settings' `reminder_*`/`notify_*` toggles yet (TD-28) |
| **Settings → Library Profile** | `routes/setting.py` (`library_profile`, `remove_library_logo`, `_save_upload`) + `database/branding_storage.py` (logo/stamp/signature file storage) | `database/settings_queries.py` (Supabase, ADR-24) | `templates/settings/library_profile.html` | `css/settings.css`, `js/settings.js` | `library_settings` (Supabase, no SQLite mirror) — **note (added 2026-07-26, ADR-38):** also owns `morning_capacity`/`afternoon_capacity`/`evening_capacity` (Seating Capacity card, read by Occupancy Analytics via `database/bi_queries.py`'s `get_shift_capacities()`). `create_library_settings()`/`update_library_settings()` retry once without these three fields if the live database doesn't have those columns yet. **As of 2026-08-28 (ADR-57):** logo/stamp/signature upload to a public Supabase Storage bucket (`library-branding`, auto-created) via `database/branding_storage.py`; `library_settings.*_path` holds the full public URL; templates render it through `utils/branding.py`'s `branding_src()` Jinja global (which also resolves any legacy `uploads/settings/…` row). Works on Render and Vercel; TD-73 resolved. Run `scripts/migrate_branding_to_storage.py` once against a deployment that has pre-existing local images |
| **Settings → Membership Settings (plan pricing/policy)** | `routes/setting.py` (`membership_settings`) | `database/membership_settings_queries.py` (Supabase, ADR-24), `database/notification_settings_queries.py` (read-only, for the Reminders & Notifications summary) | `templates/settings/membership_settings.html` | — | `membership_settings` (Supabase, no SQLite mirror) — **note:** plan duration/fee/admission-fee now read by `routes/membership.py`'s create/renew flow as of 2026-07-21 via `database/membership_queries.py` (TD-7 partially resolved); `late_fee_per_day`/`renewal_grace_days`/`auto_expiry`/`allow_early_renewal` still unread anywhere. `reminder_days`/`send_reminders` no longer written here (TD-23) — see Notification Settings row below |
| **Settings → Receipt Settings (numbering/branding/printing) / Receipt Preview** | `routes/setting.py` (`receipt_settings`) | `database/receipt_settings_queries.py` (Supabase, ADR-24) — same `get_receipt_settings()` also used by `routes/payment.py`'s `receipt()` as of 2026-07-25 | `templates/settings/receipt_settings.html` — "Receipt Preview" card includes `components/receipt_document.html` (added 2026-07-25, ADR-33 — same partial the real post-payment receipt page uses) | `css/settings.css`, `js/settings.js` | `library_settings` (Supabase, no SQLite mirror) — **note:** `receipt_prefix`/`next_receipt_number` are now actually consumed by `database/payment_queries.py`'s `generate_receipt_number()` (Supabase read/advance as of ADR-24 — TD-22 resolved, ADR-13; TD-40 open, see [11_FUTURE_WORK.md](11_FUTURE_WORK.md)). `auto_increment_receipt` is still inert — the counter always advances regardless, since no manual-receipt-number entry field exists to make "off" meaningful yet. As of 2026-07-25, `paper_size`/`print_logo`/`print_stamp`/`print_signature`/`receipt_footer` are consumed by the real receipt page (TD-22 partially resolved further); `auto_print`/`auto_email`/`open_pdf_after_save`/`duplicate_copy` remain unread. **To change the receipt's visual design, edit only `components/receipt_document.html`** — both this preview and the real receipt page pick up the change automatically |
| **Notification/reminder behavior (reminder days, channels, quiet hours, dashboard-display toggles)** | `routes/setting.py` (`notification_settings`); also read by `routes/dashboard.py` (`dashboard`, for `dash_show_pending_fees`) and `app.py` (`inject_notification_summary`, for the navbar bell's `nav_notification_prefs`) | `database/notification_settings_queries.py` (Supabase, ADR-24) | `templates/settings/notification_settings.html`, `templates/settings/membership_settings.html` (read-only summary), `templates/components/notification_dropdown.html` (badge/category visibility), `templates/dashboard/index.html` (Pending Fees card) | `css/settings.css`, `js/settings.js` (quiet-hours enable/disable) | `library_settings` (Supabase, no SQLite mirror) — **note:** single owner of this behavior as of 2026-07-21 (ADR-8); reminder-rule/channel/quiet-hours toggles are save-only, nothing dispatches SMS/Email/WhatsApp yet (TD-24); `dash_show_new_admissions` has no widget yet (TD-25) |
| **Settings → Staff & User Access** *(placeholder — PF-4)* | `routes/setting.py` (`staff_access`) | none | `templates/settings/staff_access.html` | `css/settings.css` (`.settings-coming-soon-*`, `.settings-role-card`) | — |
| **Backup export logic (Data & Backup)** | `routes/setting.py` (`data_backup`, `backup_export_csv`, `backup_create`, `_collect_admin_backup_data`) | `database/backup_queries.py` (Supabase, ADR-24); `backup_export_csv()` also reads Supabase `students` directly (ADR-24) | `templates/settings/data_backup.html` | `css/settings.css` | `backup_log` (Supabase, no SQLite mirror) — **note:** manual export/backup only, no scheduled backups yet (PF-5). As of 2026-07-25 (ADR-32), `backup_create()` exports this admin's own rows across every admin-scoped Supabase table as JSON instead of copying the (now-deleted) SQLite file — this fixed **TD-32**, a critical cross-tenant leak. As of 2026-08-28 (ADR-56), it streams that JSON straight from memory (`send_file(io.BytesIO(...))`) with no `backups/` file, so it works on a read-only filesystem too (mitigates the backup half of TD-73) |
| **Security/session preferences (Security Settings)** | `routes/setting.py` (`security_settings`) — password-change branch touches `admins` via `database/supabase_client.py` (Supabase, as of 2026-07-23, ADR-17 — was raw SQLite until this cutover) | `database/security_settings_queries.py` (Supabase, ADR-24) | `templates/settings/security_settings.html` | `css/settings.css`, `js/settings.js` (password match check) | `security_settings` (Supabase, no SQLite mirror) — **note:** password change is fully enforced; `session_timeout_minutes`/`remember_me_enabled`/`login_notifications_enabled` are persisted but **not enforced** anywhere yet (TD-26). Writes Supabase's `admins.password`, same copy `routes/auth.py`'s login reads (TD-35 `Resolved`) — a password changed here now takes effect on the next login. |
| **Reports** | `routes/report.py` — permanent redirect shim | — | `templates/reports/index.html` exists but is dead | — | — |
| **Receipt numbering (actual generation)** *(centralized 2026-07-22, see ADR-13)* | n/a — pure data-access, called from `routes/payment.py` (`collect`, `receipt`) and `routes/membership.py` (`create`/`renew`) | `database/payment_queries.py` (`generate_receipt_number`, `record_payment`) — reads `receipt_prefix`/`next_receipt_number` from `library_settings` and advances the counter in the same transaction as the payment; `get_payment_id_by_receipt_number` (added 2026-07-25) resolves the `payment_id` a receipt number belongs to, without widening `record_payment()`'s own return shape — see ADR-33 | — | — | `payments.receipt_number`, `library_settings.next_receipt_number` — **note:** one implementation, not three; changing the format changes it everywhere at once, don't reintroduce a per-route inline formula |
| **Auth/session guard pattern** | Copy-pasted `if "admin_id" not in session: return redirect("/")` at the top of nearly every route function — no decorator or `before_request` hook exists yet | — | — | — | — |
| **Colors / design tokens** | — | — | — | `css/style.css` (`.sidebar-wrapper` vars), `css/business_intelligence.css` (`--bi-*`), `css/ai_center.css` (`--ai-*`), `css/membership_distribution.css` (`--md-*`), `css/panda.css` (`--panda-*`) — each page redefines its own tokens, not a single shared file | — | — |
| **Database schema changes** | — | `database/schema.sql` (fresh installs) **+** a new `database/migrate_*.py` script (existing installs) — there's no migration runner, both must be updated together | — | — | update [04_DATABASE_SCHEMA.md](04_DATABASE_SCHEMA.md) in the same change |
| **Global Search (navbar)** *(added 2026-08-17, ADR-44)* | `routes/search.py` (`suggestions`) | `database/search_queries.py` (`search_students`, `search_payments`, `search_enquiries`, `search_cashbook`, `global_search`) — reuses `database.membership_queries.get_admin_students`, `database.payment_queries.get_payments_for_admin`, `database.cashbook_queries.get_cashbook_ledger`'s own `search=` param; `search_enquiries()` is the one new direct `.eq("admin_id", ...)` read, `enquiries` having no reusable query function before this | `templates/layouts/navbar.html` (`#globalSearch` widget) | `css/navbar_search.css`, `js/navbar_search.js` (both loaded globally from `layouts/base.html`, `js/navbar_search.js` gated to logged-in sessions same as `js/panda.js`) | `students`, `payments`, `enquiries`, `cashbook` (Supabase, all admin_id-scoped — see ADR-2) — **note:** a live, grouped-by-type autocomplete (Students/Payments/Enquiries/Cashbook), same explicit-selection-only pattern ADR-41 established for AI Center's student search; each type is capped at `RESULTS_PER_TYPE=5`. Cashbook results have no single-record link — there's no `GET`-viewable single-entry Cashbook page yet (TD-56); to change what a suggestion shows for a given type, edit that type's function in `search_queries.py` and its matching branch in `routes/search.py`'s `suggestions()`, not the JS |

| **Provisioning a new library (one-deployment-per-library pilot model)** *(added 2026-08-21, ADR-53)* | n/a — operator process, not a route | `scripts/verify_schema.py` (confirms every table exists on a fresh Supabase project before go-live), `scripts/verify_tenant_isolation.py` (confirms a project contains only one library's data, before and after the first admin registers) | — | — | [PROVISIONING.md](PROVISIONING.md) is the step-by-step runbook — **note:** each pilot library gets its own fully separate app deployment and its own fully separate Supabase project; there is no `firm_id`/`organization_id`/tenant column and no Admin/Reception role system anywhere in this codebase (explicitly deferred, see ADR-53 in [DECISIONS.md](DECISIONS.md)) — don't add one without a fresh decision to do so |

## If your change doesn't fit neatly into one row above

- Check [FILE_REFERENCE.md](FILE_REFERENCE.md) for the specific file's full dependency card (what it depends on, what depends on it) before editing.
- Check [09_DEPENDENCY_MAP.md](09_DEPENDENCY_MAP.md) for cross-blueprint `url_for` couplings that won't show up as Python imports.
- Check [11_FUTURE_WORK.md](11_FUTURE_WORK.md) — your change might already be a known, tracked issue.
