# Where to Modify X

Quick lookup: find the feature you want to change, get every file it touches. For deeper detail on any file, jump to its card in [FILE_REFERENCE.md](FILE_REFERENCE.md).

| Feature | Route(s) | Data access | Templates | Static | Table(s) |
|---|---|---|---|---|---|
| **Login / Register / Forgot Password** | `routes/auth.py` | Supabase (PostgreSQL) via `database/supabase_client.py` on `admins` — as of 2026-07-23 (ADR-16); was raw SQLite until this cutover. `register()` used to also mirror-insert into SQLite (`database/db.py`) so other tables' SQLite FKs to `admins.admin_id` kept resolving for new admins; as of 2026-07-25 (ADR-31) that mirror-insert is deleted outright — Supabase is now the only store, and this file has zero SQLite dependency | `templates/auth/*.html` (extends `auth_base.html`) | `css/login.css`, `js/login.js` | `admins` (Supabase-only as of ADR-31. `password` also has a single writer again as of 2026-07-23 — see TD-35 `Resolved`: `routes/setting.py`'s password-change form writes the same Supabase copy, ADR-17) |
| **Dashboard (KPIs, quick actions)** | `routes/dashboard.py` | Supabase (PostgreSQL) via `database/membership_queries.py` (`get_membership_counts`, `get_memberships_for_admin`, `get_admin_students`, `get_days_left`) for students/enquiries/memberships — as of 2026-07-23 (ADR-23). `utils/charts.py`'s `generate_revenue_chart` and `database/cashbook_queries.py`'s `get_total_fee_revenue`/`get_today_fee_collection`/`get_pending_fees` all Supabase too as of 2026-07-24 (ADR-25) — this route has no SQLite dependency left at all | `templates/dashboard/index.html` + `components/{dashboard_header,quick_actions,revenue_chart,membership_chart,expiry_table,recent_admissions,*_modal}.html` | `js/dashboard-charts.js`, `js/transaction_modal.js` | `students`, `memberships`, `enquiries`, `payments` (all Supabase, ADR-23/25) |
| **Enquiries** | `routes/enquiries.py` | Supabase (PostgreSQL) via `database/supabase_client.py` on `enquiries` — as of 2026-07-23 (ADR-18), and as of 2026-07-24 (ADR-30) the only store, no SQLite mirror left | `templates/enquiries/*.html` | — | `enquiries` (Supabase-only as of ADR-30, no SQLite mirror left. `status` has a single writer — see TD-36 `Resolved`: `routes/student.py`'s `admission()` flips `status` in Supabase directly, ADR-19) |
| **Students / Admissions** | `routes/student.py` | Supabase (PostgreSQL) via `database/supabase_client.py` on `students` — as of 2026-07-23 (ADR-19), and as of 2026-07-24 (ADR-29) the only store, no SQLite mirror left; `view()`'s `memberships`/`payments` reads also Supabase as of 2026-07-24 (ADR-25) | `templates/students/*.html` | — | `students` (Supabase-only as of ADR-29, no SQLite mirror left), `enquiries` (`admission()` reads/writes this via Supabase directly too, same copy `routes/enquiries.py` owns — closes TD-36), `memberships`, `payments` (all Supabase-only, ADR-25/28/29) |
| **Memberships (create/renew)** | `routes/membership.py` | Supabase (PostgreSQL) via `database/supabase_client.py` on `memberships` — as of 2026-07-23 (ADR-20), and as of 2026-07-24 (ADR-29) the only store, no SQLite mirror left; also uses `database/payment_queries.py` (`record_payment` — Supabase-only and strict, ADR-28), `database/membership_settings_queries.py` (`get_membership_settings`), and `database/membership_queries.py` (`get_active_membership` — now Supabase-backed, ADR-20 — plus `get_effective_status`, `get_plan_pricing`, `get_admission_fee`) | `templates/memberships/{index,create,renew}.html` | — | `memberships` (Supabase is authoritative for `index()`/`create()`/`renew()`, and, as of 2026-07-23 (ADR-21), also for `routes/payment.py`'s `collect()` — Supabase-only as of ADR-29, no SQLite mirror left; TD-37 `Resolved`), `payments`, `cashbook`, `audit_log` (all Supabase-only, ADR-22/25/27/28) — **note:** `create()` now redirects to `renew()` instead of creating a second membership if one is already active (2026-07-21); plan duration/fee auto-fill from `membership_settings` (TD-7 partially resolved); as of 2026-07-22, both routes reject negative `paid_amount`/`due_amount` and roll back on any DB error mid-write (TD-22 resolved) |
| **Membership Distribution (plan analytics)** | `routes/membership_distribution.py` | Supabase (PostgreSQL) via `database/membership_queries.py` (`get_membership_counts`, `get_memberships_for_admin`, `get_effective_status`) + `utils/charts.py` (`generate_membership_distribution_donut`) — as of 2026-07-23 (ADR-23). `receipt_number`/`payment_mode`/`payment_date`/`last_amount_paid` now come from `database/payment_queries.get_payments_for_admin()` (Supabase) as of 2026-07-24 (ADR-25) — this route has no SQLite dependency left at all. `get_total_fee_revenue`/`get_pending_fees` (`database/cashbook_queries.py`, both Supabase) | `templates/memberships/distribution.html` + `components/membership_*.html` | `css/membership_distribution.css`, `js/membership_distribution.js` | `memberships`, `students`, `payments` (all Supabase, ADR-23/25) |
| **Membership effective status / days-to-expiry (single source of truth)** | n/a — pure data-access, called from Memberships, Dashboard, Student profile, Membership Distribution, Notifications | `database/membership_queries.py` (`EFFECTIVE_STATUS_SQL`, `get_effective_status`, `DAYS_LEFT_SQL`, `get_membership_counts`, `get_active_membership`) | — | — | `memberships` — **note:** `membership_status` never auto-flips to `'Expired'` in the DB; this module is the one place that combines it with `end_date` to get the real status (ADR-12, resolves TD-6). Changing this changes what "Active"/"Expired" means everywhere at once — don't reintroduce a local copy of the date comparison |
| **Input normalization (Category/Name/Location/free-text/phone formatting, single source of truth)** *(added 2026-07-26, ADR-36)* | n/a — pure string functions, called from Enquiries, Students, Memberships, Payments, Library Profile | `utils/normalization.py` (`normalize_category` — Purpose/Shift/Membership Plan/Payment Mode → UPPERCASE; `normalize_name`/`normalize_location` — person names/City/State → Title Case; `normalize_free_text` — Address/Remarks/Receipt Footer, trim/collapse only; `normalize_phone` — trim/collapse only). One-time backfill for pre-existing rows: `database/migrate_normalize_input_data.py` (re-runnable, hand-run via `python -m database.migrate_normalize_input_data`) | `templates/students/edit.html` (Purpose/Shift `<select>` pre-selection compares against the UPPERCASE stored value) | — | `enquiries`, `students`, `memberships`, `payments`, `library_settings` — **note:** a new field that's a fixed-vocabulary category / person name / city-state / free text / phone number should call the matching function here, not a local `.upper()`/`.title()`/`.strip()`. `routes/membership_distribution.py`'s `plan_counts`/`utils/charts.py`'s `PLAN_CHART_COLORS` both read `memberships.plan_name` post-normalization — see their own notes above/below if you touch plan-name matching. Cashbook's own `category`/`payment_method` fields are deliberately **not** covered — see TD-48 in [11_FUTURE_WORK.md](11_FUTURE_WORK.md) before normalizing those |
| **Fee revenue / pending fees (single source of truth)** | n/a — pure data-access, called from Dashboard, Membership Distribution, Cashbook and Business Intelligence | `database/cashbook_queries.py` (`get_total_fee_revenue`/`get_today_fee_collection` — Supabase `students`/`payments` via a local `_fetch_payments_for_admin()`, as of 2026-07-24, ADR-25; `get_pending_fees` — Supabase `students`/`memberships` via `database.membership_queries.get_memberships_for_admin()`, as of 2026-07-23, ADR-23) | — | — | `payments`, `memberships`, `students` (all Supabase, ADR-23/25) — **note:** deliberately distinct from `get_total_income()` (all Cashbook income, including non-fee categories); see ADR-11 in [DECISIONS.md](DECISIONS.md). Changing either function changes Dashboard's "Total Revenue"/"Pending Fees", Membership Distribution's "Revenue Collected"/"Pending Payments", Cashbook's "Pending Fees", and the BI health score's collection component all at once — that's the point, don't reintroduce a second copy in any one of them |
| **Membership Analytics** *(redirect shim — fixed 2026-07-22, see PF-2)* | `routes/membership_analytics.py` | none | none — redirects to `membership_distribution.index` | — | — |
| **Payments (collect fee) / Receipt page** | `routes/payment.py` | Supabase (PostgreSQL) via `database/supabase_client.py` on `memberships` for `collect()`'s balance read/update — as of 2026-07-23 (ADR-21), and as of 2026-07-24 (ADR-29) the only store, no SQLite mirror left. `index()` reads Supabase `payments`/`students` via `database/payment_queries.get_payments_for_admin()` as of 2026-07-24 (ADR-25); `collect()`/`create()`/`renew()` use `database/payment_queries.py`'s `record_payment()` (Supabase-only and strict as of 2026-07-24, ADR-28) plus, as of 2026-07-25, `get_payment_id_by_receipt_number()` to find the new redirect target; the new `receipt(payment_id)` (added 2026-07-25) reads `payments`/`students`/`memberships` directly and this admin's `library_settings` via `database/receipt_settings_queries.get_receipt_settings()` | `templates/payments/{index,collect,receipt}.html` — `receipt.html` includes `components/receipt_document.html` (added 2026-07-25, ADR-33) | `css/settings.css` (`.receipt-preview-doc` rules, `receipt.html` only) | `memberships` (Supabase-only as of ADR-29, no SQLite mirror left), `payments` (Supabase-only as of ADR-28, no SQLite mirror left), `students` (Supabase-only as of ADR-29, no SQLite mirror left), `cashbook`, `audit_log` (Supabase-only, ADR-27/ADR-26) — **note:** as of 2026-07-22, `collect()` refuses to run against a membership with no pending balance and rolls back on any DB error mid-write (TD-22 resolved). As of 2026-07-25, `collect()`/`create()`/`renew()` redirect to `payment.receipt` (real receipt page) instead of `student.view` whenever a payment was actually recorded — see ADR-33 |
| **Cashbook (ledger, manual entries, audit log)** | `routes/cashbook.py` | Supabase (PostgreSQL) via `database/supabase_client.py` on `cashbook`/`audit_log`, through `database/cashbook_queries.py`/`database/audit_queries.py` — as of 2026-07-23 (ADR-22), and as of 2026-07-24 (ADR-27) Supabase is the **only** store for `cashbook`, no SQLite mirror left at all. Manual entries (`insert_transaction`) write the `cashbook` row first and roll it back if the `audit_log` insert then fails; automatic entries (`insert_income_entry`, triggered by Membership/Payment) wrap ID generation and both inserts in one best-effort `try/except: pass` (see TD-43 — as of ADR-27 this has no SQLite fallback left, unlike TD-39/TD-41's staleness-only risk) — `payment_id` **is** sent to Supabase for those too since 2026-07-24 (ADR-25), now that `payments` itself has a Supabase row (closing TD-38's common case). Also `database/cashbook_categories.py` (constants) | `templates/cashbook/index.html` + `components/cashbook_*.html` | `css/cashbook.css`, `js/cashbook.js`, `js/transaction_modal.js` | `cashbook` (Supabase-only as of 2026-07-24, ADR-27 — the SQLite mirror-write was removed entirely, the second mirror actually deleted, not just migrated), `audit_log` (Supabase-only as of 2026-07-24, ADR-26 — the first mirror actually deleted; `cashbook_queries.py` writes the Supabase `audit_log` row directly) |
| **Business Intelligence — Overview (health score, growth, action items)** | `routes/business_intelligence.py` (`index`) | `database/bi_queries.py` (Supabase, ADR-23, for its 3 membership-side functions; the rest reuses `database/cashbook_queries.py`, unchanged) | `templates/business_intelligence/index.html` + `components/bi_*.html` + `components/bi_components.html`'s `tabs()` macro (added 2026-07-25) | `css/business_intelligence.css`, `js/business_intelligence.js` | `cashbook`, `memberships`, `students` (Supabase, ADR-22/23) |
| **Business Intelligence — Purpose Analytics** *(real data as of 2026-07-26, Phase 1)* | `routes/business_intelligence.py` (`purpose_analytics`, builder `_purpose_analytics_data`) | `database/bi_queries.py` (`get_purpose_breakdown` — merges `database.membership_queries.get_admin_students`/`database.payment_queries.get_payments_for_admin` in Python) | `templates/business_intelligence/purpose_analytics.html`, built from `components/bi_components.html` macros — unchanged, consumes whatever `data` the route passes | `css/business_intelligence.css` (extended, same file Overview uses), `js/bi_purpose_analytics.js` — unchanged | `students` (`purpose`, `status`), `payments` (`amount_paid`, `student_id`) (Supabase) |
| **Business Intelligence — Revenue Analytics** *(real data as of 2026-07-26, Phase 2, ADR-37)* | `routes/business_intelligence.py` (`revenue_analytics`, builder `_revenue_analytics_data`) | `database/bi_queries.py` (`get_revenue_time_windows`, `get_monthly_fee_revenue`, `get_revenue_by_plan`, `get_revenue_by_payment_mode`, `get_payment_mode_usage_counts`, `get_new_vs_renewal_revenue`, `get_revenue_collection_summary`, `get_avg_revenue_per_student`, plus the existing `get_purpose_breakdown`) — merges `database.payment_queries.get_payments_for_admin`/`database.membership_queries.get_memberships_for_admin`/`get_admin_students` in Python | `templates/business_intelligence/revenue_analytics.html`, built from `components/bi_components.html` macros — gained a second KPI row and two new chart cards (`paymentModeChart`, `newVsRenewalChart`) plus a Business Insights row in this same change | `css/business_intelligence.css` (extended, same file Overview uses), `js/bi_revenue_analytics.js` (extended with two new `renderPie()` calls) | `payments` (`amount_paid`, `payment_date`, `payment_mode`, `membership_id`, `student_id`), `memberships` (`plan_name`, `total_fee`, `paid_amount`, `pending_amount`, `membership_id`, `student_id`) (Supabase) — **note:** deliberately Payments/Memberships-scoped, not Cashbook's broader `get_monthly_income()` total (ADR-11/ADR-37); New-vs-Renewal is classified by `membership_id` order per student, not Cashbook's best-effort `source` tag — see ADR-37 before changing either |
| **Business Intelligence — Occupancy Analytics** *(real data as of 2026-07-26, Phase 3, ADR-38)* | `routes/business_intelligence.py` (`occupancy_analytics`, builder `_occupancy_analytics_data`) | `database/bi_queries.py` (`get_shift_capacities`, `get_active_memberships`, `get_shift_occupancy`, `get_occupancy_summary`, `get_occupancy_by_purpose`, `get_occupancy_by_plan`, `get_purpose_shift_matrix`, `get_monthly_occupancy_trend`, `get_occupancy_insights`) — merges `database.membership_queries.get_memberships_for_admin`/`get_effective_status` with `database.settings_queries.get_library_settings` in Python | `templates/business_intelligence/occupancy_analytics.html`, built from `components/bi_components.html` macros plus `components/bi_occupancy_insights.html` (new) — gained a second KPI row, three new chart cards, an AI Insights section, two insight cards, and a second table (Purpose Shift Matrix) in this same change | `css/business_intelligence.css` (extended, same file Overview uses), `js/bi_occupancy_analytics.js` (extended with three new render functions) | `students` (`shift`, `purpose`), `memberships` (`membership_status`, `end_date`, `joining_date`, `plan_name`, `student_id`), `library_settings` (`morning_capacity`/`afternoon_capacity`/`evening_capacity`, new) — **note:** "occupied" means a currently effectively-active membership, not every student ever admitted (deliberately narrower than Purpose Analytics — see ADR-38); capacity is a genuinely configurable admin setting, not a fabricated number, but the live database may not have the `*_capacity` columns applied yet (see the Settings → Library Profile row below and ADR-38) |
| **Notifications (expiry buckets + navbar bell)** | `routes/notification.py` (`index()` for the page, `get_notification_summary()` for the navbar bell via `app.py`'s context processor) | Supabase (PostgreSQL) via `database/membership_queries.py` (`get_memberships_for_admin`, `get_days_left`) — as of 2026-07-23 (ADR-23); was raw SQLite until this cutover. This route has no SQLite dependency left | `templates/notification/index.html`, `components/notification_dropdown.html` | — | `memberships`, `students` (Supabase, ADR-23) — **note:** bucket window (today/tomorrow/3-day/expired) is hardcoded and does not read Notification Settings' `reminder_*`/`notify_*` toggles yet (TD-28) |
| **Settings → Library Profile** | `routes/setting.py` (`library_profile`, `remove_library_logo`) | `database/settings_queries.py` (Supabase, ADR-24) | `templates/settings/library_profile.html` | `css/settings.css`, `js/settings.js` | `library_settings` (Supabase, no SQLite mirror) — **note (added 2026-07-26, ADR-38):** also owns `morning_capacity`/`afternoon_capacity`/`evening_capacity` (Seating Capacity card, read by Occupancy Analytics via `database/bi_queries.py`'s `get_shift_capacities()`). `create_library_settings()`/`update_library_settings()` retry once without these three fields if the live database doesn't have those columns yet — see that file's card and ADR-38 before assuming a save failure means something else is wrong |
| **Settings → Membership Settings (plan pricing/policy)** | `routes/setting.py` (`membership_settings`) | `database/membership_settings_queries.py` (Supabase, ADR-24), `database/notification_settings_queries.py` (read-only, for the Reminders & Notifications summary) | `templates/settings/membership_settings.html` | — | `membership_settings` (Supabase, no SQLite mirror) — **note:** plan duration/fee/admission-fee now read by `routes/membership.py`'s create/renew flow as of 2026-07-21 via `database/membership_queries.py` (TD-7 partially resolved); `late_fee_per_day`/`renewal_grace_days`/`auto_expiry`/`allow_early_renewal` still unread anywhere. `reminder_days`/`send_reminders` no longer written here (TD-23) — see Notification Settings row below |
| **Settings → Receipt Settings (numbering/branding/printing) / Receipt Preview** | `routes/setting.py` (`receipt_settings`) | `database/receipt_settings_queries.py` (Supabase, ADR-24) — same `get_receipt_settings()` also used by `routes/payment.py`'s `receipt()` as of 2026-07-25 | `templates/settings/receipt_settings.html` — "Receipt Preview" card includes `components/receipt_document.html` (added 2026-07-25, ADR-33 — same partial the real post-payment receipt page uses) | `css/settings.css`, `js/settings.js` | `library_settings` (Supabase, no SQLite mirror) — **note:** `receipt_prefix`/`next_receipt_number` are now actually consumed by `database/payment_queries.py`'s `generate_receipt_number()` (Supabase read/advance as of ADR-24 — TD-22 resolved, ADR-13; TD-40 open, see [11_FUTURE_WORK.md](11_FUTURE_WORK.md)). `auto_increment_receipt` is still inert — the counter always advances regardless, since no manual-receipt-number entry field exists to make "off" meaningful yet. As of 2026-07-25, `paper_size`/`print_logo`/`print_stamp`/`print_signature`/`receipt_footer` are consumed by the real receipt page (TD-22 partially resolved further); `auto_print`/`auto_email`/`open_pdf_after_save`/`duplicate_copy` remain unread. **To change the receipt's visual design, edit only `components/receipt_document.html`** — both this preview and the real receipt page pick up the change automatically |
| **Notification/reminder behavior (reminder days, channels, quiet hours, dashboard-display toggles)** | `routes/setting.py` (`notification_settings`); also read by `routes/dashboard.py` (`dashboard`, for `dash_show_pending_fees`) and `app.py` (`inject_notification_summary`, for the navbar bell's `nav_notification_prefs`) | `database/notification_settings_queries.py` (Supabase, ADR-24) | `templates/settings/notification_settings.html`, `templates/settings/membership_settings.html` (read-only summary), `templates/components/notification_dropdown.html` (badge/category visibility), `templates/dashboard/index.html` (Pending Fees card) | `css/settings.css`, `js/settings.js` (quiet-hours enable/disable) | `library_settings` (Supabase, no SQLite mirror) — **note:** single owner of this behavior as of 2026-07-21 (ADR-8); reminder-rule/channel/quiet-hours toggles are save-only, nothing dispatches SMS/Email/WhatsApp yet (TD-24); `dash_show_new_admissions` has no widget yet (TD-25) |
| **Settings → Staff & User Access** *(placeholder — PF-4)* | `routes/setting.py` (`staff_access`) | none | `templates/settings/staff_access.html` | `css/settings.css` (`.settings-coming-soon-*`, `.settings-role-card`) | — |
| **Backup export logic (Data & Backup)** | `routes/setting.py` (`data_backup`, `backup_export_csv`, `backup_create`, `_collect_admin_backup_data`) | `database/backup_queries.py` (Supabase, ADR-24); `backup_export_csv()` also reads Supabase `students` directly (ADR-24) | `templates/settings/data_backup.html` | `css/settings.css` | `backup_log` (Supabase, no SQLite mirror) — **note:** manual export/backup only, no scheduled backups yet (PF-5). As of 2026-07-25 (ADR-32), `backup_create()` exports this admin's own rows across every admin-scoped Supabase table as JSON instead of copying the (now-deleted) SQLite file — this fixed **TD-32**, a critical cross-tenant leak |
| **Security/session preferences (Security Settings)** | `routes/setting.py` (`security_settings`) — password-change branch touches `admins` via `database/supabase_client.py` (Supabase, as of 2026-07-23, ADR-17 — was raw SQLite until this cutover) | `database/security_settings_queries.py` (Supabase, ADR-24) | `templates/settings/security_settings.html` | `css/settings.css`, `js/settings.js` (password match check) | `security_settings` (Supabase, no SQLite mirror) — **note:** password change is fully enforced; `session_timeout_minutes`/`remember_me_enabled`/`login_notifications_enabled` are persisted but **not enforced** anywhere yet (TD-26). Writes Supabase's `admins.password`, same copy `routes/auth.py`'s login reads (TD-35 `Resolved`) — a password changed here now takes effect on the next login. |
| **Reports** | `routes/report.py` — permanent redirect shim | — | `templates/reports/index.html` exists but is dead | — | — |
| **Receipt numbering (actual generation)** *(centralized 2026-07-22, see ADR-13)* | n/a — pure data-access, called from `routes/payment.py` (`collect`, `receipt`) and `routes/membership.py` (`create`/`renew`) | `database/payment_queries.py` (`generate_receipt_number`, `record_payment`) — reads `receipt_prefix`/`next_receipt_number` from `library_settings` and advances the counter in the same transaction as the payment; `get_payment_id_by_receipt_number` (added 2026-07-25) resolves the `payment_id` a receipt number belongs to, without widening `record_payment()`'s own return shape — see ADR-33 | — | — | `payments.receipt_number`, `library_settings.next_receipt_number` — **note:** one implementation, not three; changing the format changes it everywhere at once, don't reintroduce a per-route inline formula |
| **Auth/session guard pattern** | Copy-pasted `if "admin_id" not in session: return redirect("/")` at the top of nearly every route function — no decorator or `before_request` hook exists yet | — | — | — | — |
| **Colors / design tokens** | — | — | — | `css/style.css` (`.sidebar-wrapper` vars), `css/business_intelligence.css` (`--bi-*`), `css/membership_distribution.css` (`--md-*`) — each page redefines its own tokens, not a single shared file | — | — |
| **Database schema changes** | — | `database/schema.sql` (fresh installs) **+** a new `database/migrate_*.py` script (existing installs) — there's no migration runner, both must be updated together | — | — | update [04_DATABASE_SCHEMA.md](04_DATABASE_SCHEMA.md) in the same change |

## If your change doesn't fit neatly into one row above

- Check [FILE_REFERENCE.md](FILE_REFERENCE.md) for the specific file's full dependency card (what it depends on, what depends on it) before editing.
- Check [09_DEPENDENCY_MAP.md](09_DEPENDENCY_MAP.md) for cross-blueprint `url_for` couplings that won't show up as Python imports.
- Check [11_FUTURE_WORK.md](11_FUTURE_WORK.md) — your change might already be a known, tracked issue.
