# Future Work, Known Technical Debt, and Stubs

This file has two distinct kinds of content — don't conflate them:

- **Known Technical Debt** — real defects/inconsistencies already in the codebase today. This is a **living log**: add a new `TD-N` row the moment you find a new one (during any coding session, not just a dedicated audit), and flip its `Status` to `Resolved` (don't delete the row) once it's fixed, with a note pointing at the [CHANGELOG.md](CHANGELOG.md) entry that fixed it.
- **Stub Routes & Planned Features** — intentionally incomplete work (flash-a-message placeholders, empty-context pages) that isn't a bug, just unfinished.

Everything below was discovered by reading the actual code (verified 2026-07-20), not inferred.

## Known Technical Debt

| ID | Issue | Severity | Location | Found On | Status |
|---|---|---|---|---|---|
| TD-1 | Chart PNGs written to shared, non-admin-scoped filenames — one admin can briefly see another's chart data. As of 2026-08-18, `routes/dashboard.py`'s new period-aware `revenue_chart()` endpoint (Revenue Overview's This Year/Last Year switcher) writes to the same shared `static/charts/revenue.png`, adding a same-admin race too (a slow "This Year" regeneration finishing after a "Last Year" one could momentarily overwrite it back) | Medium (real cross-tenant data leak, low likelihood window) | `utils/charts.py`, `routes/dashboard.py` | 2026-07-20 | **Resolved** — 2026-08-28 (ADR-56): server-side chart PNGs removed entirely. `utils/charts.py` is deleted; the Dashboard/Distribution charts now render client-side with Chart.js from `{labels, datasets}` payloads built per-request by `utils/chart_data.py`'s `build_*_chart_data(admin_id, ...)` and scoped to the logged-in admin. Nothing is written to `static/charts/` (or any path) any more, so there is no shared file to leak or race on. See the 2026-08-28 CHANGELOG entry. |
| TD-2 | `transactions` table defined twice with incompatible shapes (`schema.sql` vs `migrate_transactions.py`) | Medium (latent — table unused today, but a landmine for whoever wires it up) | `database/schema.sql`, `database/migrate_transactions.py` | 2026-07-20 | **Resolved** — 2026-07-25 (ADR-32, Phase 11): both competing SQLite definitions are gone (`schema.sql`/`migrate_transactions.py` deleted outright along with the rest of SQLite). Supabase's `database/supabase_migration.sql` defines `transactions` exactly once, with no competing shape — the table remains genuinely unused by any route or query module, but the landmine (two disagreeing definitions) no longer exists. |
| TD-3 | `expenses` table exists but nothing reads or writes it | Low | `database/supabase_migration.sql` (was `database/schema.sql`, deleted 2026-07-25, ADR-32) | 2026-07-20 | Open |
| TD-4 | Legacy `settings` table (non-admin-scoped) superseded by `library_settings` but still created | Low | `database/supabase_migration.sql` (was `database/schema.sql`, deleted 2026-07-25, ADR-32) | 2026-07-20 | Open |
| TD-5 | `PRAGMA foreign_keys = ON` only runs once at schema-init; regular request connections never enforce FKs | Medium | `database/db.py`, `database/schema.sql` | 2026-07-20 | **Resolved** — 2026-07-25 (ADR-32, Phase 11): both files are deleted along with SQLite. Postgres (Supabase) enforces every FK unconditionally on every connection — there is no per-connection `PRAGMA` step to forget, and no equivalent gap exists in the new backend. |
| TD-6 | `membership_status` never auto-flips to `'Expired'` on `end_date` passing — "effective status" recomputed independently in 3+ files | Medium (duplication risk — the 3 copies could drift) | `routes/dashboard.py`, `routes/student.py`, `routes/membership_distribution.py` | 2026-07-20 | **Resolved** — a 4th copy (`routes/membership.py`'s `index()`) was found actually drifted (showing "Active" for a membership whose `end_date` had passed) during the 2026-07-21 Membership Workflow audit; all four now share `database/membership_queries.py`'s `EFFECTIVE_STATUS_SQL`/`get_effective_status`. See [CHANGELOG.md](CHANGELOG.md). |
| TD-7 | `membership_settings` (plan pricing/policy) configured via Settings but not read by Membership create/renew | Medium (feature gap, not a bug) | `routes/membership.py`, `database/membership_settings_queries.py` | 2026-07-20 (introduced alongside the 2026-07-20 Membership Settings feature) | **Partially Resolved** (2026-08-17, ADR-45) — plan duration/fee/admission-fee flow from `membership_settings` into `create()`/`renew()` via `database/membership_queries.py`'s `get_plan_pricing`/`get_admission_fee`, and as of 2026-08-17 `total_fee` for a standard plan is now server-computed and authoritative (no longer client-editable auto-fill — see ADR-45) — closing the specific bug where the admission fee got silently folded into a client-typed total instead. `late_fee_per_day`, `renewal_grace_days`, `auto_expiry` and `allow_early_renewal` remain unconsumed — none has any existing UI/automation surface to attach to (no late-fee line item, no grace-window check, no auto-expiry job, no early-renewal block), and adding one of those was judged "a new feature", out of scope for a pricing-correctness fix. See [CHANGELOG.md](CHANGELOG.md). |
| TD-8 | `requirements.txt` missing `matplotlib`/`numpy`, which `utils/charts.py` imports | High (breaks a fresh install immediately) | `requirements.txt`, `utils/charts.py` | 2026-07-20 | **Resolved** — verified 2026-07-22 (QA & Validation Sprint) that `requirements.txt` already lists both `matplotlib>=3.7.0` and `numpy>=1.26.0`; this row was stale (fixed in an untracked commit sometime between 2026-07-20 and this verification, with no changelog entry). No code change made this session — doc correction only. |
| TD-9 | `config.py`'s `Config`/`DevelopmentConfig`/`ProductionConfig` never imported by `app.py` | Low (dead code, mildly confusing) | `config.py`, `app.py` | 2026-07-20 | **Resolved** — verified 2026-07-25 that `app.py` already imports and applies `DevelopmentConfig`/`ProductionConfig` via `app.config.from_object(...)` (wired in the untracked "Production hardening and QA complete" commit, `c911386`, with no changelog entry); this row was stale. Additionally, `config.py` was missing a `load_dotenv()` call, so `.env` was never actually loaded into `os.environ` before config values were read — fixed this session, see [CHANGELOG.md](CHANGELOG.md). |
| TD-10 | `database/seed.py` doesn't seed data (name is misleading) and imports `generate_password_hash` unused | Low | `database/seed.py` | 2026-07-20 | **Resolved** — 2026-07-25 (ADR-32, Phase 11): `database/seed.py` is deleted along with the rest of SQLite. There's no misleadingly-named script or unused import left to fix. |
| TD-11 | Unwired templates rendered by no route: `payments/create.html`, ~~`payments/success.html`~~, `cashbook/transactions.html`, `cashbook/analytics.html`, `reports/index.html` | Low | see [FILE_REFERENCE.md](FILE_REFERENCE.md) template cards | 2026-07-20 | **Partially Resolved** — 2026-07-25 (Receipt Template Consolidation): `payments/success.html` is deleted; the real post-payment receipt page (`payments/receipt.html`, wired to `routes/payment.py`'s new `receipt()`) supersedes it. `payments/create.html`, `cashbook/transactions.html`, `cashbook/analytics.html`, `reports/index.html` remain unwired — see [CHANGELOG.md](CHANGELOG.md). |
| TD-12 | `templates/components/alert.html` is a 0-byte empty file | Low | `templates/components/alert.html` | 2026-07-20 | Open |
| TD-13 | `routes/auth.py` hardcodes `role="Admin"` on registration; schema default is lowercase `'admin'` | Low (bites the first case-sensitive `role` check) | `routes/auth.py`, `database/supabase_migration.sql` (was `database/schema.sql`, deleted 2026-07-25, ADR-32) | 2026-07-20 | Open |
| TD-14 | `routes/enquiries.py`'s `delete()` runs on a plain `GET`, no confirmation | Medium (accidental/crawler-triggered deletion risk) | `routes/enquiries.py` | 2026-07-20 | Open |
| TD-15 | `templates/layouts/sidebar.html` expects badge-count context vars (`enquiries_new_count`, etc.) that nothing supplies | Low (cosmetic — badges render blank) | `templates/layouts/sidebar.html` | 2026-07-20 | Open |
| TD-16 | Six folders are completely empty with zero references anywhere: `models/`, `services/`, `reports/`, `tests/`, `backups/`, `.agents/` | See TD-20 for `tests/` specifically | project root | 2026-07-20 | **Partially Resolved** (2026-07-22) — `tests/` is no longer empty, see TD-20. `models/`, `services/`, `reports/`, `backups/` (holds runtime-generated files only), `.agents/` remain empty. |
| TD-17 | No `.gitignore`; 38 `.pyc` files committed to git, producing noisy binary diffs | Low (hygiene) | repo root, `__pycache__/` dirs | 2026-07-20 | Open |
| TD-18 | Root `README.md` is empty (0 bytes) | Low | `README.md` | 2026-07-20 | Open |
| TD-19 | `if "admin_id" not in session: return redirect("/")` duplicated in nearly every route function instead of centralized | Medium (easy to forget on a new route, silently exposing it) | every `routes/*.py` except `report.py`/`membership_analytics.py` | 2026-07-20 | Open |
| TD-20 | No automated tests exist at all (`tests/` empty) | High (highest-leverage gap — fee/receipt/health-score logic has no regression safety net) | `tests/` | 2026-07-20 | **Resolved** (2026-07-22, QA & Validation Sprint) — added a 251-test pytest suite (`tests/conftest.py` + `tests/test_00..09_*.py`) covering Auth, Enquiries, Students/Admission, Membership create/renew, Payments, Cashbook, Dashboard, BI, Membership Analytics/Distribution, Notifications, Settings (all 7 sub-pages), cross-tenant IDOR isolation, and the full Enquiry→...→Audit Log workflow chain with DB-consistency assertions, run via the Flask test client against a real (git-restorable) copy of `library.db`. `pytest` added as a dev dependency (not yet in `requirements.txt` — it's a test-only dependency; add a `requirements-dev.txt` if that distinction matters going forward). See [CHANGELOG.md](CHANGELOG.md). |
| TD-21 | `database/migrate_*.py` scripts import via bare `from db import get_connection` instead of `from database.db import get_connection` — only works because they're always run standalone | Low (fragile if ever imported as a module instead of run as a script) | every `database/migrate_*.py` | 2026-07-20 | **Resolved** — 2026-07-25 (ADR-32, Phase 11): every `database/migrate_*.py` script (and `database/db.py` itself) is deleted outright along with the rest of SQLite. There is no bare-import inconsistency left to fix. |
| TD-22 | `library_settings`'s receipt columns (`receipt_prefix`, `next_receipt_number`, `paper_size`, `auto_print`, `auto_email`, etc.) are configured via Settings but not read by any receipt-generating code — same shape as TD-7 | Medium (feature gap, not a bug) | `routes/setting.py`'s `receipt_settings()`, `routes/membership.py`, `routes/payment.py` | 2026-07-20 (introduced alongside the 2026-07-20 Receipt Settings feature) | **Resolved (partially, receipt_prefix/next_receipt_number/print_logo/print_stamp/print_signature/paper_size/receipt_footer only)** — 2026-07-22 Payment Workflow Audit centralized receipt generation into `database/payment_queries.py`'s `generate_receipt_number()`, which now reads and advances `receipt_prefix`/`next_receipt_number` from `library_settings` (ADR-13). 2026-07-25 (Receipt Template Consolidation): the new real post-payment receipt page (`routes/payment.py`'s `receipt()` → `templates/payments/receipt.html`) reads this admin's `library_settings` via `get_receipt_settings()` and renders `print_logo`/`print_stamp`/`print_signature`/`receipt_footer`/branding through the shared `components/receipt_document.html` partial, the same one Settings → Receipt Settings' preview uses — see ADR-33 in [DECISIONS.md](DECISIONS.md). `paper_size` is stored but has no printed-page-size effect yet (browser print dialog controls that). `auto_print`/`auto_email`/`open_pdf_after_save`/`duplicate_copy` remain unread — no auto-print-on-load, email-dispatch, or server-side PDF generation exists anywhere to consume them (a deliberate scope boundary of the 2026-07-25 fix, which uses the browser's native `window.print()` rather than adding a PDF pipeline); see TD-29 for the one receipt column (`auto_increment_receipt`) that's read but deliberately not honored. See [CHANGELOG.md](CHANGELOG.md). |
| TD-23 | `membership_settings.reminder_days`/`send_reminders` columns are dead/unused — superseded by `library_settings`'s `reminder_*`/`notify_*` columns (Notification Settings), but kept in the table for backward compatibility rather than dropped | Low (unused columns, not a correctness bug) | `database/supabase_migration.sql` (was `database/schema.sql`, deleted 2026-07-25, ADR-32), `database/membership_settings_queries.py` | 2026-07-21 (introduced alongside the 2026-07-21 Notification Settings feature) | Open |
| TD-24 | No SMS/Email/WhatsApp dispatch engine exists anywhere in the codebase — Notification Settings' channel toggles, all reminder-rule toggles, and quiet-hours are persisted preferences only; nothing currently sends anything | Medium (feature gap, not a bug — same shape as TD-22's `auto_email`) | `routes/setting.py`'s `notification_settings()`, `database/notification_settings_queries.py` | 2026-07-21 | Open |
| TD-25 | `dash_show_new_admissions` has no corresponding dashboard widget/notification category to attach to yet — unlike `dash_show_pending_fees` (really does hide/show the dashboard's "Pending Fees" card) and the badge/today/tomorrow/overdue toggles (really do affect the navbar bell dropdown) | Low (inert toggle, not a bug) | `routes/dashboard.py`, `templates/dashboard/index.html` | 2026-07-21 | Open |
| TD-26 | `security_settings.session_timeout_minutes`/`remember_me_enabled`/`login_notifications_enabled` are persisted but not enforced — no session-expiry middleware exists, no login-notification delivery mechanism exists | Medium (feature gap, not a bug; password change itself is fully functional) | `routes/setting.py`'s `security_settings()`, `database/security_settings_queries.py` | 2026-07-21 | Open |
| TD-27 | Dashboard's "Library Highlights" card duplicates 2 of its 4 tiles (Active Memberships, Pending Fees) verbatim from the KPI card row above it on the same page — same number, same admin, no added context. Found during the 2026-07-21 Dashboard Integrity Audit; left as-is since removing/restructuring cards is a visible information-hierarchy change beyond that audit's correctness/consistency/performance scope | Low (redundant, not incorrect — both figures are correct and now share one source of truth, see ADR-11) | `templates/components/library_insights.html`, `templates/dashboard/index.html` | 2026-07-21 | Open |
| TD-28 | Notification Settings' reminder-rule toggles (`reminder_7_days`, `reminder_3_days`, `reminder_1_day`, `notify_on_expiry_day`, `notify_after_expiry`) are cosmetic — `routes/notification.py`'s `get_notification_summary()` always computes the same fixed today/tomorrow/3-day/expired buckets for the navbar bell and Notifications page regardless of which of these an admin has disabled. Distinct from TD-24 (channel dispatch, e.g. SMS/Email, doesn't exist at all) — this is about the in-app bell/page content itself not respecting the rule toggles that already control it in the UI | Medium (admin-facing setting with no observable effect) | `routes/notification.py`, `routes/setting.py`'s `notification_settings()` | 2026-07-21 (found during the Membership Workflow audit) | Open |
| TD-29 | `library_settings.auto_increment_receipt` is read but not honored — `database/payment_queries.py`'s `generate_receipt_number()` always advances `next_receipt_number` after issuing a receipt, regardless of this toggle, because no manual-receipt-number entry field exists anywhere in the payment/membership forms. Turning the toggle off in Settings → Receipt Settings has no observable effect | Low (inert toggle, not a bug — see ADR-13's Consequence section for why "off" can't mean "don't advance" without a manual-entry UI to fall back to) | `database/payment_queries.py`, `routes/setting.py`'s `receipt_settings()` | 2026-07-22 (found during the Payment Workflow Audit, surfaced while resolving TD-22) | Open |
| TD-30 | No idempotency/double-submit protection on any payment-creating form (Admission, Renewal, Collect Payment) — a double-click, browser back-button resubmit, or network retry on the same POST can create two distinct `payments` rows (and two matching Cashbook entries) for what the admin intended as one payment. Each row gets its own valid, non-colliding receipt number, so nothing detects or rejects the duplicate | Medium (real double-booking risk, no detection mechanism exists) | `routes/payment.py`'s `collect()`, `routes/membership.py`'s `create()`/`renew()` | 2026-07-22 (found during the Payment Workflow Audit) | **Resolved** — 2026-08-21 (ADR-53): a per-page-load `idempotency_key` hidden field, enforced by a `UNIQUE` column on `payments`/`memberships`, is checked *before* any balance update or insert at all three call sites. Degrades silently (no dedup, not a failure) on a Supabase project where the column hasn't been manually added yet — confirmed live against this repo's own dev/test project, where the new tests self-skip rather than fail. See the 2026-08-21 changelog entry. |
| TD-32 | `POST /settings/backup/create` (`routes/setting.py`'s `backup_create()`) copies and serves the **entire shared `database/library.db` file**, not just the requesting admin's own data — unlike every other query in the app, which is scoped by `admin_id`. Confirmed live: downloading a backup while logged in as one admin produces a SQLite file containing every other admin's `students`/`enquiries`/`memberships`/`payments`/`cashbook`/`audit_log` rows, plus the full `admins` table (usernames, mobiles, bcrypt password hashes) for every admin in the system, not just the one who clicked the button. This is the same *category* of cross-tenant leak as TD-1 (shared chart PNGs) but far more severe — TD-1 exposes a transient chart image, this exposes a portable, permanent, downloadable copy of every other tenant's complete financial and personal data, on request, with no server-side scoping at all. Reproduced via the Flask test client (`tests/test_07_settings.py::test_backup_create_leaks_entire_multi_tenant_database`) | **Critical** (full cross-tenant data exposure — every admin can download every other admin's students, payments, and password hashes on demand) | `routes/setting.py`'s `backup_create()`, `database/backup_queries.py` | 2026-07-22 (QA & Validation Sprint) | **Resolved** — 2026-07-25 (ADR-32, Phase 11): removing SQLite entirely forced exactly option (a) predicted below — `backup_create()` now calls a new `_collect_admin_backup_data(admin_id)` helper that queries every admin-scoped Supabase table filtered to this admin only (`.eq("admin_id", ...)` for tables with that column; `.in_("student_id", ...)` against this admin's own student ids for `memberships`/`payments`, which have none), plus this admin's own `admins` profile row with `password` explicitly excluded from the selected columns. Downloads a per-admin JSON export instead of the old whole-file `.db` snapshot. `tests/test_07_settings.py::test_backup_create_leaks_entire_multi_tenant_database` (documented the leak) replaced with `test_backup_create_scoped_to_own_admin_only` (proves the fix: seeds two admins, asserts one's export contains none of the other's data and no password anywhere). See the 2026-07-25 "Removed SQLite entirely" changelog entry and ADR-32 in `DECISIONS.md`. |
| TD-33 | `database/bi_queries.py`'s `get_business_health_score()` docstring states "A library with no data yet gets neutral 0.5 on components it has no signal for, rather than being punished with a 0" — true for the growth, fee-collection, and membership-renewal components (each has an explicit `else 0.5` branch for the no-data case), but **not** true for the expense-discipline component: `classify_expense_health()`'s zero-income branch (`ratio = 1.0 if current_expense > 0 else 0.0`) scores a library with truly zero income *and* zero expense this month as a perfect 0% spend ratio (`expense_component = 1.0`, the best possible score), not the documented neutral 0.5. A brand-new library with no financial activity at all therefore lands on a Business Health Score of 65 ("Good"), not the 50 ("Average") its own docstring implies, purely from this one component. Confirmed via `tests/test_05_dashboard_bi_analytics.py::test_bi_health_score_no_data_matches_documented_formula` | Low (cosmetic — a display number reads more favorably than the code's own stated design intent for a genuinely inactive library; doesn't affect any financial total, and "no expenses is good" is itself a defensible reading, just not the one the docstring describes) | `database/bi_queries.py` (`classify_expense_health`, `get_business_health_score`) | 2026-07-22 (QA & Validation Sprint) | Open — left as a documented inconsistency rather than unilaterally changed, since which behavior is "correct" (perfect score vs. neutral score for zero activity) is a product/business judgment call, not a clear-cut bug; whoever owns the BI module should either special-case the zero/zero case to return 0.5, or update the docstring to describe the actual formula |
| TD-34 | `database/supabase_migration.sql` (new, see ADR-14) is a hand-maintained PostgreSQL mirror of `database/schema.sql` — the two files have no automated sync, so any future table/column/constraint change applied to one and not the other will silently drift, the same risk category as TD-2's `transactions` table | Now real for `admins`, `enquiries`, `students`, `memberships`, `cashbook`, and `audit_log` specifically (Medium); still Low/latent for the other 8 tables, none of which any route reads from Supabase yet | `database/schema.sql`, `database/supabase_migration.sql` | 2026-07-22 | **Resolved** — 2026-07-25 (ADR-32, Phase 11): `database/schema.sql` is deleted along with the rest of SQLite. There is no second schema file left to drift out of sync with — `database/supabase_migration.sql` is now the single, sole source of truth for the app's schema. (Historical trail before this: 2026-07-23 (ADR-16): `routes/auth.py` became the first route to actually read/write a Supabase table (`admins`). 2026-07-23 (ADR-18/19/20): `enquiries`/`students`/`memberships` followed. 2026-07-23 (ADR-22): `cashbook`/`audit_log` followed. See TD-35/TD-36/TD-37 for the related (but distinct) **data** split-brain risks these cutovers introduced, all now `Resolved`, and TD-38/TD-39 for the same category of risk ADR-22 introduced.) |
| TD-31 | `flash()` messages set by a route are only ever actually displayed on 11 of the app's ~25 top-level pages — every other page extends `layouts/base.html`, which itself never calls `get_flashed_messages()`, and the page has no local copy either. Confirmed live via the Flask test client: `payment.collect()`'s own "Amount cannot exceed pending balance" validation error, and its success flash, were both being set correctly and then silently discarded — the exact same request rendered its target template with 200/redirected with 302 as designed, just with no visible feedback at all. Fixed 2026-07-22 for the 7 templates that are actual redirect/render targets in the Payment workflow (`students/{index,view}.html`, `payments/{index,collect}.html`, `memberships/{index,create,renew}.html`); confirmed still missing on `dashboard/index.html`, `cashbook/index.html`, `business_intelligence/index.html`, `notification/index.html`, `memberships/{distribution,analytics}.html`, `students/{edit,admission}.html`, `enquiries/{add,edit,view}.html`, `settings/staff_access.html` — none of these call `get_flashed_messages()` either, so any `flash()` call reachable from those pages (redirects into Cashbook edits, Enquiry CRUD, Student edit/admission, etc.) is equally invisible today | High (every one of those routes' error/success messages is currently silent — a save can fail with a flashed reason the user never sees, or succeed with no confirmation at all) | `templates/layouts/base.html` and the ~14 page templates listed above that extend it with no local `get_flashed_messages()` block | 2026-07-22 (found during the Payment Workflow Audit while verifying "Success messages"/"Error messages" against the live app, not by code reading alone) | **Resolved** (2026-07-22, QA & Validation Sprint) — the remaining 12 templates (`dashboard/index.html`, `cashbook/index.html`, `business_intelligence/index.html`, `notification/index.html`, `memberships/distribution.html`, `students/{edit,admission}.html`, `enquiries/{add,edit,view}.html`, `settings/staff_access.html`) all gained the same `get_flashed_messages()` block used by the 18 pages that already had one. `memberships/analytics.html` is excluded — it's no longer rendered by any route (see PF-2). Every reachable page in the app now displays its own flashes; the `layouts/base.html`-centralization alternative this row originally floated wasn't taken (would mean touching all 30 templates instead of the remaining 12, for the same end result) — still open as a maintainability idea, not a defect, if a future refactor wants one copy instead of 30. See [CHANGELOG.md](CHANGELOG.md). |

| TD-36 | `enquiries.status` now has two writers that never sync: `routes/student.py`'s `admission()` sets it to `'Admitted'` in **SQLite only** (that route is out of scope for the Enquiries migration, ADR-18), while `routes/enquiries.py`'s `index()`/`edit()`/`view()` now read `status` from **Supabase only** (source of truth as of ADR-18). After a student is admitted, Supabase's `enquiries.status` stays stale (e.g. `"Interested"`) even though SQLite's flips to `"Admitted"` — the Enquiries list/detail pages keep showing the pre-admission status and an "Admission" action instead of the "Admitted" badge and "View Student" link, even though `student_id` (read separately from SQLite) correctly reflects the admission. A direct, deliberate consequence of migrating Enquiries before Students (ADR-18), same shape as TD-35's `admins.password` split but on a different table/column. Related, separate risk on the same table: `routes/enquiries.py`'s `delete()` deletes from Supabase (authoritative, always succeeds/fails cleanly) then best-effort deletes the SQLite mirror row, swallowing `sqlite3.Error` — if a student was already admitted from that enquiry, `students.enquiry_id`'s SQLite FK (no `ON DELETE` clause) blocks the SQLite delete, leaving an orphaned SQLite row (harmless — no route reads it once it's gone from Supabase — but a leftover, not a clean state) | Medium (real, user-visible UI staleness for every admission going forward; the delete edge case is a rarer, lower-impact leftover-row issue, not a crash) | `routes/student.py`'s `admission()`, `routes/enquiries.py`'s `index()`/`edit()`/`view()`/`delete()` | 2026-07-23 (found while migrating `routes/enquiries.py` to Supabase, ADR-18) | **Resolved** — 2026-07-23 (ADR-19): `routes/student.py`'s `admission()` now reads/updates `enquiries` via `database/supabase_client.py`'s `get_supabase_client()` (the same client `routes/enquiries.py` uses) instead of the SQLite mirror, and its `status='Admitted'` write now targets Supabase directly — a single writer again, matching TD-35's resolution shape. The delete-orphan edge case described above is unrelated to this fix and remains exactly as described (harmless, unread once gone from Supabase). See the 2026-07-23 "Migrated `routes/student.py`" changelog entry. |
| TD-37 | `memberships.paid_amount`/`pending_amount` now have two writers that never sync: `routes/payment.py`'s `collect()` updates them in **SQLite only** (that route is out of scope for the Membership migration, ADR-20), while `routes/membership.py`'s `index()` now reads `memberships` from **Supabase only** (source of truth as of ADR-20). After a payment is collected against an existing membership, Supabase's copy of `paid_amount`/`pending_amount` stays stale (the pre-payment values) even though SQLite's copy is updated correctly — the Memberships page keeps showing the old balance until the same membership is next written by `create()`/`renew()` (which a payment collection never triggers). Same shape as TD-35's `admins.password` split and TD-36's `enquiries.status` split, on a different table/columns | Medium (real, user-visible balance staleness on the Memberships page after every payment collection against an existing membership; every other page that shows this balance — Dashboard, Membership Distribution, Notifications, Student detail — still reads SQLite directly and is unaffected) | `routes/payment.py`'s `collect()`, `routes/membership.py`'s `index()` | 2026-07-23 (found while migrating `routes/membership.py` to Supabase, ADR-20) | **Resolved** — 2026-07-23 (ADR-21): `routes/payment.py`'s `collect()` now updates `paid_amount`/`pending_amount` in Supabase directly (the same copy `routes/membership.py`'s `index()` reads), before mirroring the same update into SQLite for the other still-unmigrated readers. `memberships.paid_amount`/`pending_amount` have a single writer-of-record again. See the 2026-07-23 "Migrated `routes/payment.py`" changelog entry. |
| TD-38 | `cashbook.payment_id` no longer round-trips through Supabase for automatic entries (`insert_income_entry()`, `database/cashbook_queries.py`) — Supabase's `cashbook` table has a live PostgreSQL FK to `payments`, and `payments` is still SQLite-only (unmigrated), so sending a real `payment_id` to Supabase always fails with a `23503` FK violation (verified live before this design was chosen). The SQLite mirror still gets the real `payment_id` (reconciliation there is unaffected), but any code that wants to trace "which payment produced this ledger entry" via the Supabase copy of `cashbook` cannot — it must query the SQLite mirror instead | Medium (real capability gap in the new source of truth, not a crash — the SQLite-side reconciliation this exists for, ADR-13, still works) | `database/cashbook_queries.py`'s `insert_income_entry()` | 2026-07-23 (found while migrating Cashbook to Supabase, ADR-22) | **Resolved** — 2026-07-24 (ADR-25): closed exactly as predicted, automatically, once `payments` migrated to Supabase — `insert_income_entry()` now sends a real `payment_id` to Supabase `cashbook`, and the FK resolves cleanly since a matching `payments` row now exists there too. |
| TD-39 | `insert_income_entry()`'s Supabase mirror-write is best-effort (wrapped in a bare `try/except: pass`) rather than the strict "write source of truth first, roll back on mirror failure" shape every other migrated write uses — because its caller chain (`database/payment_queries.py`'s `record_payment()`, called from `routes/membership.py`/`routes/payment.py`) is out of this slice's scope and only catches `sqlite3.Error`. In the rare case of a transient Supabase failure at the moment of an automatic entry, the underlying payment/membership write still succeeds and the SQLite mirror still gets the correct row, but that one automatic Cashbook entry (and its audit-log row) can be briefly — or, if never retried, permanently — missing from the Supabase-sourced Cashbook ledger/KPIs/audit trail an admin actually looks at, with nothing surfacing the gap | Medium (a real, if narrow, staleness window in the new source of truth; bounded to genuine Supabase outages, not structural like TD-35/36/37 were) | `database/cashbook_queries.py`'s `insert_income_entry()` | 2026-07-23 (found while migrating Cashbook to Supabase, ADR-22) | Superseded by **TD-43** — `payments`/`routes/membership.py`/`routes/payment.py` did migrate (ADR-25/28/29), but that made `insert_income_entry()`'s own best-effort write *more* consequential, not less: as of ADR-27 there's no SQLite fallback left at all, so a Supabase failure here now means real, permanent data loss rather than temporary staleness. TD-43 documents that current state; this row's original framing (staleness that "closes" once migration finishes) no longer applies. See TD-43. |
| TD-40 | `database/payment_queries.py`'s `generate_receipt_number()` advances Supabase `library_settings.next_receipt_number` (ADR-24) as its own immediate write, no longer inside the same SQLite transaction as the `payments` row it's issued for (`payments` itself is still SQLite-only, unmigrated). If that enclosing SQLite transaction later rolls back (e.g. the `payments` `INSERT` hits an unlikely receipt-number collision, or `insert_income_entry()` raises before the caller's `conn.commit()`), the Supabase counter has already advanced and does not roll back with it — the next payment then skips a number, which is harmless for `payments.receipt_number` uniqueness (numbers are just skipped, never reused) but means the counter no longer exactly equals "count of receipts issued". This narrows, rather than removes, a race the pre-migration design already had (the receipt-number availability loop was never fully serialized against a concurrent request either) | Low (a receipt-number gap on an already-rare rollback path, not a duplicate/collision — `_receipt_number_taken()`'s uniqueness check is unaffected) | `database/payment_queries.py`'s `generate_receipt_number()` | 2026-07-24 (found while migrating Settings to Supabase, ADR-24) | Open — **not** closed by `payments` itself migrating (ADR-25), nor by `payments`' SQLite mirror-write being removed (ADR-28): the counter advance is still a separate Supabase write, made before the `payments` insert even runs, not part of one atomic transaction with it — as of ADR-28 both are Supabase writes, but still two separate ones, not one. Actually closing this needs both writes issued as a single Postgres transaction (most likely a database function/RPC — a schema change, out of scope for a straight mirror-write removal). |
| TD-41 | `database/payment_queries.py`'s `record_payment()` best-effort mirrors `payments` into Supabase (wrapped in `try/except postgrest.exceptions.APIError: pass`) rather than the strict "write source of truth first, roll back on mirror failure" shape — same reason as TD-39: it's called mid-transaction from `routes/membership.py`/`routes/payment.py`, both out of scope for ADR-25, and only catch `sqlite3.Error`. In the rare case of a transient Supabase failure at the moment of a payment, the SQLite write (and the underlying membership/cashbook writes) still succeed, but that one payment can be briefly — or, if never retried, permanently — missing from the Supabase-sourced `payments` reads (`routes/payment.py`'s `index()`, `routes/student.py`'s `view()`, the revenue chart, fee-revenue totals, membership listing's receipt column). If that same payment's Supabase mirror-write failed, `insert_income_entry()`'s own best-effort `payment_id` send to Supabase `cashbook` (TD-38's fix) also fails via FK violation, compounding into the same cashbook-side gap TD-39 already describes for that row | Medium (a real, if narrow, staleness window in the new source of truth; bounded to genuine Supabase outages, not structural) | `database/payment_queries.py`'s `record_payment()` | 2026-07-24 (found while migrating Payments to Supabase, ADR-25) | **Resolved** — 2026-07-24 (ADR-28): `record_payment()`'s SQLite write was removed outright and its Supabase insert made strict (raises `APIError` on failure instead of swallowing it), with `routes/membership.py`/`routes/payment.py` widened to catch it — a payment failure now rolls back cleanly instead of silently producing a Supabase-side gap. |
| TD-42 | Supabase `enquiries`/`students`/`memberships` were each missing a large block of rows SQLite had (493/433/339 respectively) — rows created before their route migrated to Supabase (ADR-18/19/20) and never edited since, so no post-migration write ever re-synced them. The original one-time bulk import (ADR-15) ran once, early, before these tables had their full history. This meant every Supabase read of these three tables since their respective ADR (Dashboard, Membership Distribution, Notifications, BI, Enquiries, Students pages) had been silently incomplete the entire time — an admin whose enquiry/student/membership predated their table's migration could see it missing entirely from a Supabase-backed page, with no error | High while open (silent, wrong data on live pages — not a crash, which is why it went undetected across 5 prior migration slices) | `enquiries`, `students`, `memberships` (Supabase) | 2026-07-24 (found while backfilling `payments` for ADR-25 — the `payments` backfill's FK violations against missing `memberships` rows were the first symptom to surface it) | **Resolved** — `database/migrate_backfill_mirror_parity.py` (new) reconciled all three tables (plus `payments`) to full SQLite parity in FK-safe order, verified 1996/1996, 1376/1376, 922/922, 1064/1064. See ADR-25. No code change needed beyond the backfill itself — the route-level migrations' read logic was always correct, they were just reading an incomplete table. |
| TD-43 | `database/cashbook_queries.py`'s `insert_income_entry()` is best-effort (bare `try/except: pass`, same shape as TD-39) but, as of 2026-07-24 (ADR-27, cashbook's SQLite mirror-write removed), has **no SQLite fallback left** — before ADR-27, a Supabase failure here still left a real row in the SQLite mirror (invisible to the app's Supabase-backed reads, but recoverable); as of ADR-27, a Supabase failure means the automatic `cashbook` entry and its `audit_log` row for that one payment are **not recorded anywhere at all**, permanently, unless manually re-created. Same root cause as TD-39/TD-41 (the caller chain — `database/payment_queries.py`'s `record_payment()`, called from `routes/membership.py`/`routes/payment.py` — is out of this migration's scope and only catches `sqlite3.Error`), but the consequence is now data loss, not staleness | High (silent, permanent data loss on a genuine Supabase outage, however rare — the underlying payment/membership write still succeeds, so nothing else fails or flags it) | `database/cashbook_queries.py`'s `insert_income_entry()` | 2026-07-24 (found while removing `cashbook`'s SQLite mirror-write, ADR-27) | **Resolved** — 2026-08-21 (ADR-53): `insert_income_entry()` now makes one bounded retry before giving up; if it still fails, `record_payment()` logs the gap and best-effort flags the payment row (`payments.cashbook_synced = FALSE`, a new column) instead of losing it silently, and `routes/cashbook.py` shows a reconciliation banner whenever any exist. The payment itself is never rolled back for this — see ADR-53 for why. Degrades silently (no flag, same as before) on a Supabase project where `cashbook_synced` hasn't been manually added yet. See the 2026-08-21 changelog entry. |
| TD-44 | `database/payment_queries.py`'s `generate_receipt_number()` fallback branch (no Library Profile / no `library_settings` row) computed its starting sequence number as `1001 + <this admin's own payment count>`, then called `_receipt_number_taken()` in a `while` loop to skip forward past any number already claimed by *any* admin. Before ADR-28, `_receipt_number_taken()` queried local SQLite (microseconds per check, so even a long skip-forward was fast); as of ADR-28, it queries Supabase — a real network round trip per check. With every admin defaulting to the shared `"LIB"` prefix until they customize it, and this fallback starting from 1001 for every fresh admin regardless of how many *other* admins have already claimed numbers, the loop had to walk one network round trip past every already-claimed `"LIB-NNNNN"` number before reaching a free one. Reproduced live: 1281 already-claimed `"LIB-"` receipts (accumulated test history) made a single fresh admin's first receipt-number allocation take **161 seconds** (1281 sequential round trips) — this was the actual root cause of the cascading, seemingly-random test failures/timeouts seen while verifying ADR-28 (mis-diagnosed at first as SQLite/Supabase ID drift and stray process contention, both of which were real but secondary — see ADR-28's revision note) | **Critical while open** (every fresh-admin payment creation degraded linearly with the library's entire historical `"LIB-"` receipt count — in production, a library that never customizes its receipt prefix and accumulates thousands of receipts would see this same multi-minute stall on every payment) | `database/payment_queries.py`'s `generate_receipt_number()` | 2026-07-24 (found while verifying ADR-28's test suite — the payments SQLite mirror-write removal) | **Resolved**, same slice — added `_max_claimed_sequence()`, a single `ORDER BY receipt_number DESC LIMIT 1` query exploiting the fixed-width zero-padded format (`%05d`) to sort lexicographically identical to numeric order, giving the true global max in one round trip. Both `generate_receipt_number()` branches (the `library_settings`-backed counter and the fallback) now seed their starting point from this value instead of a fixed floor, so the `_receipt_number_taken()` loop only ever runs for a genuine, rare collision. Verified live: the same reproduction case dropped from 161s to 0.72s. See the revised ADR-28 in `DECISIONS.md`. |
| TD-35 | `admins.password` now has two independent writers that never sync each other — `routes/auth.py`'s `forgot_password()` updates it in **Supabase only**, while `routes/setting.py`'s `security_settings()` (Settings → Security Settings → Change Password) still updates it in **SQLite only**. (`register()` does *not* have this problem — see below, it writes both.) A direct, deliberate consequence of migrating the app to Supabase one module at a time (ADR-16), starting with auth only, per that module's explicit incremental-migration scope | High (real functional bug: a password changed via Settings has no effect on login, since login checks the Supabase copy that write never reached; a password reset via `forgot_password()` similarly never reaches SQLite, so `security_settings()`'s "current password" check would still expect the old one) | `routes/auth.py`'s `forgot_password()`, `routes/setting.py`'s `security_settings()` | 2026-07-23 (found while migrating `routes/auth.py` to Supabase) | **Resolved** — 2026-07-23: `routes/setting.py`'s `security_settings()` password-change branch (`form_type == "password"`) now reads/writes `admins.password` via the same `database/supabase_client.py`'s `get_supabase_client()` that `routes/auth.py` uses, instead of raw SQLite. `admins.password` has a single writer (Supabase) again — a password changed via Settings takes effect on the next login, and a password reset via `forgot_password()` is correctly seen by Security Settings' "current password" check. See the 2026-07-23 "Migrated `routes/setting.py`'s password-change branch to Supabase" changelog entry. **Related but distinct from a second, more severe issue this same cutover surfaced and *did* get fixed in this session:** `register()` creates a brand-new `admin_id` that **7** other still-SQLite tables enforce a real foreign key against (verified directly in `database/schema.sql`, not inferred — `database/db.py` sets `PRAGMA foreign_keys = ON` on every connection): `enquiries` (`routes/enquiries.py`), `students` (`routes/student.py`), `audit_log` (written transitively from **three** modules — `routes/cashbook.py`, `routes/membership.py`, and `routes/payment.py` all call `database/cashbook_queries.py`'s `insert_income_entry()`/`insert_transaction()`, which call `audit_queries.log_entry()`), `library_settings`, `membership_settings`, `backup_log`, and `security_settings` (all four owned by `routes/setting.py`). `cashbook`/`expenses` also carry an `admin_id` column but were added via `ALTER TABLE ADD COLUMN`, which SQLite can't attach a `FOREIGN KEY` to — so those two are **not** part of this dependency, only the 7 listed above are. A new admin that existed only in Supabase would get `sqlite3.IntegrityError: FOREIGN KEY constraint failed` the instant they touched any of those 7 tables' features (reproduced live via the full test suite: 74 failures across `test_04_cashbook.py`/`test_05_dashboard_bi_analytics.py`/`test_07_settings.py`/`test_09_full_workflow_chain.py`, all tracing back to a `new_admin` fixture that now only exists in Supabase). Fixed by having `register()` insert the same row into SQLite too, under the same explicit `admin_id`, immediately after the Supabase insert succeeds (rolling back the Supabase row if the SQLite mirror fails) — see the 2026-07-23 changelog entry. This mirror is itself temporary scaffolding, not a long-term design: it can only be deleted once all 7 of `enquiries`/`students`/`audit_log`/`library_settings`/`membership_settings`/`backup_log`/`security_settings` are migrated to Supabase too — migrating Settings alone (`routes/setting.py`) only closes 4 of the 7 (`library_settings`/`membership_settings`/`backup_log`/`security_settings`); `enquiries`, `students`, and `audit_log` belong to three other, separate modules (Enquiries, Students, and Cashbook/Membership/Payment respectively) and would still require the mirror until they're migrated too. **Update (2026-07-23, ADR-18/ADR-19):** `enquiries` and `students` are both now migrated at the route level, but neither closed its FK dependency here — both needed an ongoing per-write SQLite mirror (not a one-shot bridge like this one), so `register()`'s mirror-insert is still required for both. **Update (2026-07-23, ADR-22):** `audit_log` joined them — its reads moved to Supabase too, but `log_entry()`'s SQLite mirror-write is unchanged, so it likewise didn't close its FK dependency here. `library_settings`, `membership_settings`, `backup_log`, and `security_settings` are now the only 4 tables still **fully unmigrated** among the 7 — but the bridge itself still requires all 7 unconditionally, regardless of which have a Supabase read-side cutover. **Update (2026-07-25, ADR-31):** the bridge itself — a separate mechanism from the `admins.password` split-brain this row was originally about — is now also fully resolved: `audit_log` (ADR-26), `students` (ADR-29), and `enquiries` (ADR-30) each stopped writing SQLite in turn, clearing every one of the bridge's FK dependents, and `register()`'s SQLite mirror-insert was deleted outright as the seventh and final mirror/bridge removed in Phase 10. See ADR-31 in `DECISIONS.md`. |
| TD-45 | `routes/enquiries.py`'s `edit()` has no required-field validation for `full_name`/`mobile`/`purpose`/`preferred_shift` (unlike `add()`, fixed 2026-07-25 — see [CHANGELOG.md](CHANGELOG.md)) — an admin can edit an already-admitted enquiry's fields back to blank after the fact, silently reintroducing the same incomplete-record problem the `add()`/`admission()`/membership `create()` validation was added to prevent | Low (edit-path gap, not a regression — new enquiries/admissions can no longer be created incomplete; existing ones can still be edited into that state) | `routes/enquiries.py`'s `edit()`, `templates/enquiries/edit.html` | 2026-07-25 (found while adding required-field validation to the admission workflow) | Open — deliberately left out of that session's scope (edit isn't part of "completing" an admission); should be folded into the planned "Complete Admission Details" single-form redesign rather than patched twice |
| TD-46 | Business Intelligence's three new pages (`purpose_analytics.html`, `revenue_analytics.html`, `occupancy_analytics.html`) render entirely from hardcoded placeholder dicts (`routes/business_intelligence.py`'s `_purpose_analytics_placeholder()`/`_revenue_analytics_placeholder()`/`_occupancy_analytics_placeholder()`) — every KPI, chart series, insight card, and table row is a literal, not a query. This was deliberate scope for this phase (UI/navigation framework only, no analytics calculations, no backend), not an oversight, but until it's closed these three pages show the same numbers to every admin regardless of their actual students/revenue/seats — do not mistake the "Sample data" badge on their chart/table cards for real per-admin figures | Medium (three navigable pages display fabricated business figures indistinguishable at a glance from the real Overview page's numbers, to any admin, until backed by real queries) | `routes/business_intelligence.py` (`_purpose_analytics_placeholder`, `_revenue_analytics_placeholder`, `_occupancy_analytics_placeholder`) | 2026-07-25 (BI Module UI Framework phase) | **Resolved** — 2026-07-26 (Purpose Analytics, Phase 1): `purpose_analytics()` now renders real, admin-scoped data via `_purpose_analytics_data()`/`database/bi_queries.py`'s `get_purpose_breakdown()`, replacing `_purpose_analytics_placeholder()`. `purpose_analytics.html`'s "Sample data" badge (a static macro parameter in `components/bi_components.html`, not a `data` field) was left as-is — see **TD-47** (`Resolved`). **2026-07-26 (Revenue Analytics, Phase 2):** `revenue_analytics()` now renders real, admin-scoped data via `_revenue_analytics_data()`/`database/bi_queries.py`'s revenue helpers, replacing `_revenue_analytics_placeholder()` (deleted outright, unlike Purpose Analytics' placeholder which was kept); `revenue_analytics.html`'s macro calls all pass `sample=False` in the same change, so it never regresses into TD-47's "stale Sample data badge" gap. **2026-07-26 (Occupancy Analytics, Phase 3, ADR-38):** `occupancy_analytics()` now renders real, admin-scoped data via `_occupancy_analytics_data()`/`database/bi_queries.py`'s occupancy helpers, replacing `_occupancy_analytics_placeholder()` (deleted outright); `occupancy_analytics.html`'s macro calls all pass `sample=False` too. All three pages now render real data — no placeholder remains. |
| TD-47 | `purpose_analytics.html`'s chart/table cards still show the `chart_card()`/`analytics_table()` macros' `<span class="bi-placeholder-badge">Sample data - not connected to live figures yet</span>` badge (`components/bi_components.html`) even though `purpose_analytics()` has rendered real, admin-scoped data since 2026-07-26 (Phase 1, see TD-46) — the badge is a static macro parameter, not a `data` field, and the task that wired up real data was explicitly scoped to "no template/CSS changes beyond placeholder values", so it was left as-is rather than edited on judgment call. It now visibly understates confidence in a real number | Low (cosmetic/messaging only — no wrong figures, just an unnecessarily hedged label on a page that no longer deserves it) | `templates/components/bi_components.html` (`chart_card`, `analytics_table` macros), `templates/business_intelligence/purpose_analytics.html` (the four `chart_card()` calls + one `analytics_table()` call) | 2026-07-26 (Purpose Analytics, Phase 1) | **Resolved** — 2026-07-26 (small UI polish, explicitly requested as a follow-up): both macros gained an optional `sample=True` param. `purpose_analytics.html`'s five macro calls now pass `sample=False`, rendering a new `.bi-live-badge` ("Live Data", success-green, `bi-broadcast` icon) instead of the "Sample data" badge. `revenue_analytics.html`/`occupancy_analytics.html` pass nothing, so `sample` defaults to `True` and both keep the unchanged "Sample data" badge, correctly, until their own real-data phases land. See [CHANGELOG.md](CHANGELOG.md). |
| TD-48 | Cashbook's own `category`/`payment_method` fields (`database/cashbook_categories.py`'s `MANUAL_INCOME_CATEGORIES`/`MANUAL_EXPENSE_CATEGORIES`/`PAYMENT_METHODS`, plus `database/cashbook_queries.py`'s `get_cash_balance()` hardcoded `!= "Cash"` check) were deliberately left out of the 2026-07-26 input-normalization pass (ADR-36) — they're a separate concept from the four Category fields that pass named (Purpose/Shift/Membership Plan/Payment Mode), already validated against a fixed, already-consistent Title-Case list. If a future change normalizes them too, it must cover **both** manual entries (`routes/cashbook.py`) and automatic entries (`database/payment_queries.py`'s `record_payment()` → `insert_income_entry()`) in the same change, plus `PAYMENT_METHODS`/`get_cash_balance()`'s `"Cash"` comparison/the Cashbook filter dropdown — normalizing only one write path would split `cashbook.payment_method` by capitalization between manual and automatic rows, the exact bug ADR-36 exists to prevent, just relocated into Cashbook | Low (no current inconsistency — both write paths still agree on Title Case; this is a landmine for a future partial fix, not a live bug) | `database/cashbook_categories.py`, `database/cashbook_queries.py` (`get_cash_balance`), `database/payment_queries.py` (`record_payment`), `routes/cashbook.py` | 2026-07-26 (Input Normalization) | Open — by design, not scheduled |
| TD-49 | `database/bi_queries.py`'s `get_monthly_occupancy_trend()` (Occupancy Analytics, Phase 3) approximates each past month's occupancy from `memberships.joining_date`/`end_date` range overlap, measured against **today's** seat capacity — there is no attendance/seat log to read a real historical snapshot from, and no historical capacity snapshot either. A library that changes its configured seating capacity (Settings → Library Profile → Seating Capacity) will see every past month's trend percentage silently reshape to reflect the *new* capacity, not the capacity that actually applied at the time | Low (a real, data-derived approximation, not a fabricated number — but a capacity change retroactively rewrites history on the chart, which could look like a data bug rather than the documented limitation it is) | `database/bi_queries.py` (`get_monthly_occupancy_trend`) | 2026-07-26 (Occupancy Analytics, Phase 3, ADR-38) | Open — closing this needs a historical capacity log (e.g. an audit row per Library Profile save), which is a real feature addition, not a quick fix |
| TD-50 | Occupancy Analytics' seating-capacity columns (`library_settings.morning_capacity`/`afternoon_capacity`/`evening_capacity`, ADR-38) are documented in `database/supabase_migration.sql` but this app cannot apply that `ALTER TABLE` to a live Supabase project itself (verified: no `exec_sql`/DDL RPC exists, no direct-Postgres path, ADR-14/ADR-38) — until an admin's Supabase project owner runs the exact SQL that migration file documents, in the Supabase SQL Editor, by hand, once, every admin's seating capacity silently stays at `DEFAULT_SHIFT_CAPACITY` (50/shift) regardless of what they enter in Settings → Library Profile's "Seating Capacity" card. `database/settings_queries.py`'s create/update fallback (see that file's card) prevents this from ever breaking Library Profile saving, but the entered values themselves are silently dropped, not just delayed, until the column exists | Medium (a real admin-facing feature — entering a seat capacity — silently does nothing on any Supabase project that hasn't had the migration applied, with no error surfaced anywhere in the UI) | `database/supabase_migration.sql`, `database/settings_queries.py`, `routes/setting.py` (`library_profile`) | 2026-07-26 (Occupancy Analytics, Phase 3, ADR-38) | Open — no in-app way to detect or surface "this column doesn't exist yet" to the admin without querying Postgres system catalogs (also unreachable via PostgREST); the practical fix is simply running the documented `ALTER TABLE` once per Supabase project |
| TD-51 | `database/ai_center_queries.py`'s `_payment_delay_signal()` measures "days pending" from the last actual payment made on a membership (or from `joining_date` if none), not a true days-overdue figure — `payments` has no due-date column to compare against. A student who simply hasn't been billed yet for a genuinely new charge reads identically to one who's been ignoring a bill for the same number of days | Low (the score still moves in the right direction — more elapsed time with a pending balance is still more risk — just not calibrated to an actual due date, which doesn't exist to calibrate against) | `database/ai_center_queries.py` (`_payment_delay_signal`) | 2026-07-27 (AI Center, Phase 1, ADR-39) | Open — would need a real due-date/billing-cycle column on `payments` or `memberships` to close properly; not a quick fix |
| TD-52 | AI Center's retention-risk score deliberately excludes `students.purpose` (free text — UPSC/NEET/JEE/etc.) as a signal — see ADR-39's "Why purpose is not a scoring signal." This is correct today (no outcome data exists to check a purpose/churn correlation against), but nothing currently re-checks that assumption — if enough students accumulate real outcomes (renewed vs. lapsed) over time, a purpose-based signal might become defensible and this exclusion should be revisited, not left as permanent by default | Low (a deliberately conservative omission, not a bug — the risk is it's forgotten rather than revisited) | `database/ai_center_queries.py` | 2026-07-27 (AI Center, Phase 1, ADR-39) | Open — revisit once there's enough renewal-outcome history per purpose category to check, not before |
| TD-53 | `ai_center_settings` (ADR-40 — the table AI Center Settings' weights/thresholds are persisted to) is documented in `database/supabase_migration.sql` but this app cannot create it on a live Supabase project itself (no `exec_sql`/DDL RPC, no direct-Postgres path — same constraint as TD-50, sharper here since there's no existing table to fall back to, only "doesn't exist"). Verified live: `.select()`/`.upsert()` against it both raise `APIError` code `PGRST205` until the `CREATE TABLE` is run by hand in the Supabase SQL Editor. Until then, `get_ai_center_settings()` returns `None` for every admin (→ `DEFAULTS`, so scoring is unaffected) and `save_ai_center_settings()` raises, caught by `routes/ai_center.py`'s `settings()` to flash a clear message instead of a 500 — confirmed via `tests/test_10_ai_center.py::test_ai_center_settings_save_persists_and_changes_scoring`, which `pytest.skip()`s itself when it detects that flash rather than failing | Medium (the admin-facing "change weights without touching code" feature this table exists for silently does nothing beyond showing/rejecting form input until the table is applied — same shape as TD-50, on a new feature rather than existing columns) | `database/supabase_migration.sql`, `database/ai_center_settings_queries.py`, `routes/ai_center.py` (`settings`) | 2026-07-27 (AI Center Settings, ADR-40) | **Resolved** — 2026-07-27: table created on the live project (with RLS enabled — no policies needed, see ADR-40's addendum on why). Verified live: `get_ai_center_settings()` reads real rows, `save_ai_center_settings()` persists, and `tests/test_10_ai_center.py::test_ai_center_settings_save_persists_and_changes_scoring` now passes (was previously skipping itself on the missing-table path). |
| TD-54 | The Settings page's risk-range and weight-influence bars (2026-07-27 UX pass, ADR-42) are drawn twice: once server-side (Jinja, in `templates/ai_center/settings.html`, for the initial page load) and once client-side (`static/js/ai_center_settings.js`, on every `input` event, for the live preview). Both compute the same segment-width formula independently — if one is ever changed (e.g. to round differently, or handle an out-of-range value differently), the other must be updated to match or the bars will render one way on load and redraw slightly differently the moment the admin touches a field | Low (purely cosmetic — a mismatch would look like a visual glitch, not a wrong saved value; `_parse_settings_form()` in `routes/ai_center.py` remains the single source of truth for validation, untouched by either bar) | `templates/ai_center/settings.html`, `static/js/ai_center_settings.js` | 2026-07-27 (Settings page UX pass, ADR-42) | Open — low priority; would only be worth deduplicating (e.g. server-computed widths passed as `data-*` attributes the JS reads once) if a third bar/visual is added and the duplication triples |
| TD-55 | `panda_conversations`/`panda_messages` (Panda AI Assistant chat history, ADR-43) are documented in `database/supabase_migration.sql` but this app cannot create them on a live Supabase project itself — same constraint, and same shape, as TD-53's `ai_center_settings` (no `exec_sql`/DDL RPC, no direct-Postgres path, ADR-14/38). Until a project owner runs both `CREATE TABLE` statements by hand, every `database/panda_queries.py` call raises `ChatStorageUnavailable`, and `panda/routes.py` returns a 503 with a clear message instead of a 500 — confirmed via `tests/test_11_panda.py`, whose chat-persistence tests `pytest.skip()` themselves when they detect this. Unlike TD-53, there is no `DEFAULTS`-style fallback available here — chat history genuinely does not work until the tables exist. The floating button, Today's AI Insights, and Suggested Questions are unaffected, since none of them read these two tables | Medium (a real, user-visible feature — chat history — silently 503s until the migration is applied by hand, on every fresh Supabase project) | `database/supabase_migration.sql`, `database/panda_queries.py`, `panda/routes.py` | 2026-07-27 (Panda AI Assistant, Phase 1, ADR-43) | Open — apply the two `CREATE TABLE` statements in the Supabase SQL Editor, same manual step TD-53 needed |
| TD-56 | The global navbar search's Cashbook results (`database/search_queries.py`'s `search_cashbook()`, ADR-44) are shown but not clickable, because `routes/cashbook.py` has no single-entry `GET` view route — confirmed by reading the file: `edit/<int:entry_id>` is `POST`-only, no GET branch, no view template render. `static/js/navbar_search.js`'s `selectItem()` deliberately no-ops for `type === "cashbook"` rather than linking to a page that doesn't show the entry | Low (a real, if narrow, feature gap — an admin can find a matching Cashbook entry via search but can't jump straight to it, only see it in the dropdown) | `routes/cashbook.py`, `static/js/navbar_search.js`, `database/search_queries.py` (`search_cashbook`) | 2026-08-17 (Global Search, ADR-44) | Open — closing this needs a real `GET /cashbook/view/<entry_id>` route + template, out of scope for ADR-44 (a UI-only search feature, not a Cashbook feature build-out) |
| TD-57 | Existing `memberships` rows created before the 2026-08-17 Total Fee fix (ADR-45) may hold a `total_fee` that silently absorbed the admission fee via the old buggy Create-form auto-fill, rather than matching that admin's actually-configured plan price at the time — e.g. a Monthly student showing `total_fee=2800` when the admin only ever intended `2400`. There is no `plan_id` FK from `memberships` back to `membership_settings` (by design — a membership's fee is meant to be a frozen snapshot, not one that retroactively changes if Settings changes later), so which historical rows are "wrong" can't be inferred purely from today's `membership_settings` values; settings may themselves have changed since a given row was created | Medium (financial data — a handful of installs may be showing an inflated Total Fee/Pending on old memberships until reviewed) | `database/supabase_migration.sql` (`memberships` table), `routes/membership.py` (pre-ADR-45 `create()`/`renew()`) | 2026-08-17 (found while fixing the Total Fee calculation bug, ADR-45) | Open — no automated backfill was built, deliberately: fixing this needs an admin to review flagged rows (e.g. rows whose `total_fee` doesn't cleanly equal any of that admin's plan fees, current or a plausible past value, ± admission fee) and confirm the correct historical total by hand, then a one-off `database/migrate_*.py`-style script could apply the reviewed corrections — building that script is future work, not part of ADR-45's scope (a go-forward calculation fix) |
| TD-58 | `memberships.discount_amount`/`discount_reason` (Discount feature, ADR-46) are documented in `database/supabase_migration.sql` but this app cannot apply that `ALTER TABLE` to a live Supabase project itself (same constraint as TD-50/TD-53/TD-55/TD-56 — no `exec_sql`/DDL RPC, no direct-Postgres path, ADR-14). **Unlike those precedents**, this one does not silently degrade: `database/membership_queries.py`'s `insert_membership()` raises `DiscountColumnsUnavailable` (caught by `routes/membership.py`'s `create()`, flashed as a clear "Discounts aren't available yet on this system" message) whenever a real discount (`discount_amount != 0` or a `discount_reason`) is entered before the migration has been applied — nothing is inserted, by design, since a discount is financial data and silently charging the discounted price while losing the record of why would be worse than refusing outright (see ADR-46's explicit departure from ADR-38's fallback pattern). Creating a membership with no discount (the default) is completely unaffected either way | Medium (a real, if narrow, feature gap — discounts silently refuse to apply, with a clear message, until the migration is run; core membership creation is untouched) | `database/supabase_migration.sql`, `database/membership_queries.py`, `routes/membership.py` | 2026-08-17 (Discount feature, ADR-46) | Open — apply the documented `ALTER TABLE` in the Supabase SQL Editor, same manual step TD-50/TD-53/TD-55/TD-56 needed |
| TD-59 | `panda/intents.py`'s `detect_intent()` (Panda Part 1 "Ask", ADR-47) is keyword-based — it only recognizes the specific words/phrases in its `_REVENUE_PHRASES`/`_OCCUPANCY_PHRASES`/etc. tuples. A reasonably-phrased question using none of those words (e.g. "how's business this month" instead of "revenue trend") falls to `INTENT_UNKNOWN` and gets `prompts.py`'s generic placeholder reply, even though a synonym-aware or model-based classifier would have recognized it | Low (inherent to keyword matching, not a bug — the fallback is still honest, never a wrong answer; just narrower coverage than intended) | `panda/intents.py` | 2026-08-18 (Panda Part 1 "Ask", ADR-47) | Open — expanding a keyword tuple as real phrasings surface in usage is the cheap fix; the real fix (Parts 2+, or swapping the classifier for a real model) is future work per `PANDA_SPEC.md` |
| TD-60 | `panda/insights.py`'s `get_top_risk_students()` (Panda Part 2 "Analyze", ADR-48) caps risk-scoring at `MAX_STUDENTS_SCORED_FOR_CHAT` (100) students to keep a single chat reply from making hundreds of sequential Supabase round trips. An admin with a roster larger than 100 can have real High-risk students entirely outside the scored window (whichever 100 `get_admin_students()` happens to return first) — their absence from a "which students are at risk" chat reply would look like "no risk," not "not checked" | Medium (silent incompleteness for large-roster admins only; small/typical libraries are fully covered — no wrong number is ever shown, just a potentially incomplete list) | `panda/insights.py` (`get_top_risk_students`, `MAX_STUDENTS_SCORED_FOR_CHAT`) | 2026-08-18 (Panda Part 2 "Analyze", ADR-48) | **Partially addressed 2026-08-24 (ADR-54) — the latency half only, not the coverage half.** The original per-student `compute_student_risk()` loop (3-4 Supabase round trips *per student*) measured 30+ seconds for just a 17-student roster in live testing — well under the 100-student cap, so the cap alone was never actually bounding this to something fast. `database/ai_center_queries.py`'s new `compute_student_risk_batch()` scores this admin's whole (capped) slice in a constant number of round trips instead (measured: ~17s → ~1.2s for 17 students, direct function-call timing; verified byte-identical scores against the old per-student path — see `tests/test_14_panda_reliability_and_coverage.py`). **The cap itself is completely unchanged** — an admin with >100 students still only has their first 100 scored, and TD-60's original coverage complaint (a real High-risk student outside that window still looks like "no risk") is exactly as true as before this fix. The "(scored the first 100 of N students)" honesty note this row used to ask for was actually already added on 2026-08-20 (ADR-52, `get_risk_scoring_coverage()`) — that part of this row was stale even before today. Still Open for the real remaining gap: a real aggregate risk query or paginating the scan across multiple chat turns. |
| TD-61 | `panda/insights.py`'s `get_revenue_explanation()` (Panda Part 3 "Explain", ADR-49) can only say *that* new-admissions momentum, unrenewed expirations, or pending collections moved — it can never say *which Cashbook income category* (Admission Fee vs. Renewal vs. Donation vs. Library Fine, etc.) drove a revenue change, because no function anywhere in this codebase computes a **monthly** income-category breakdown; `database/cashbook_queries.get_income_category_totals()` is all-time only. A library whose revenue actually moved because of a one-off Donation or Book Sale entry (not admissions/renewals/collection at all) gets an honest "nothing stands out" reply instead of the real reason | Low (inherent to what data already exists, not a bug — the reply never claims a category-level cause it doesn't have, it just has a narrower set of checkable signals than a full explanation would need) | `panda/insights.py` (`get_revenue_explanation`), `database/cashbook_queries.py` (`get_income_category_totals`) | 2026-08-19 (Panda Part 3 "Explain", ADR-49) | Open — closing this needs a new `get_monthly_income_category_totals()`-style query (grouping Cashbook rows by both month and category, which nothing does today) before `get_revenue_explanation()` could add a category-level factor |
| TD-62 | `panda/forecasting.py` (Panda Part 5 "Forecast", ADR-51) is a plain ordinary-least-squares straight line through at most 6 monthly data points, with no seasonality, no confidence interval, and no outlier handling — a single unusual month (a one-off bulk enrollment, a large one-time Donation entry, a data-entry mistake) skews the fitted line the same way it would skew any small-sample linear fit, and the reply reports the resulting number/direction as-is with no caveat beyond the general "not a guarantee" framing. Separately, `forecast_membership_churn()`'s lapse-rate series is indexed by *observed* month (skipping any month with zero memberships expiring), not a true calendar-continuous series — two real data points that happen to be 3 calendar months apart (because the months between had nothing expiring) are still treated as adjacent (`x` and `x+1`) in the fit, understating how much time actually separates them | Low (both are inherent to choosing a simple, transparent, no-new-dependency method over a real statistical/ML forecasting library — consistent with ADR-39/47's established preference — not a correctness bug; every reply is explicitly labeled "a simple trend projection... not a guarantee") | `panda/forecasting.py` (`_linear_forecast`, `forecast_membership_churn`) | 2026-08-19 (Panda Part 5 "Forecast", ADR-51) | Open — closing the seasonality/outlier gap would need a real forecasting library (`statsmodels`/`prophet`, currently not a dependency, see `PANDA_SPEC.md` §16) or at minimum an outlier-dampening step (e.g. median-of-residuals clipping) before fitting; closing the churn-indexing gap would need weighting each rate point by its actual elapsed-months gap rather than treating every observed month as equally spaced |

| TD-63 | `tests/conftest.py`'s `create_membership()` helper defaults `end_date` to the hardcoded literal `"2026-08-21"` (paired with a hardcoded `joining_date` of `"2026-07-22"`) instead of deriving it relative to `date.today()`. This was a valid "not expiring soon" fixture when written, but as real calendar time approaches that literal date, the gap between "today" and the hardcoded `end_date` shrinks — once today is within 3 days of `2026-08-21` (i.e. on or after 2026-08-18), any test that relies on this default membership *not* appearing in the Notifications page's 3-day-expiry window starts failing, with no code change required to trigger it. Reproduced live during the 2026-08-19 Panda QA audit's full-repo regression sweep: `tests/test_09_full_workflow_chain.py::test_full_chain_updates_every_downstream_module_exactly_once` fails its `assert b"Chain Test Student" not in resp.data` (`/notifications/`) check, confirmed reproducible in isolation (not a concurrency artifact — two other regression failures from the same sweep, `test_03_student_membership_payment.py::test_collect_payment_non_numeric_amount_rejected` and `test_08_cross_tenant_isolation.py::test_admin_b_cannot_collect_payment_on_admin_a_membership`, were both confirmed transient/environmental — they passed cleanly when rerun in isolation, unlike this one). **Not a Panda defect and not an application bug** — `routes/notification.py`'s bucketing logic is correct; this is purely test-fixture staleness, out of scope for the Panda audit that found it | Medium (breaks an unrelated, pre-existing test with zero code changes as real time passes the hardcoded date — will keep surfacing in future regression runs until fixed, and will silently stop reproducing once today's date passes `2026-08-21` entirely, which would hide it rather than fix it) | `tests/conftest.py` (`create_membership()`), surfaced via `tests/test_09_full_workflow_chain.py` | 2026-08-19 (found during the Panda QA audit's full-repo regression sweep, out of that audit's scope to fix) | **Resolved** — 2026-08-20: `create_membership()`'s default `end_date` now derives from `date.today() + timedelta(days=30)` instead of the hardcoded literal (see `docs/CHANGELOG.md`'s 2026-08-20 entry). `joining_date` was deliberately left hardcoded — `routes/membership.py`'s `create()` has no server-side cross-check between `joining_date`/`duration`/`end_date`, so only the field that actually drives the Notifications 3-day window needed to move. Verified: `tests/test_09_full_workflow_chain.py` (4/4), all 10 files calling `create_membership()` (279 tests), and a full-suite rerun (407 tests) all show the hardcoded-date failure gone, with only unrelated, confirmed-transient network errors remaining (all pass on isolated rerun) |
| TD-64 | `app.py`'s app-wide `enforce_request_security()` `before_request` hook renders an HTML error page (via `errorhandler(400)`'s `render_template("errors/error.html", ...)`) when `validate_csrf()` fails on any `POST`/`PUT`/`PATCH`/`DELETE` — including JSON `fetch()` endpoints like Panda's, which have no way to consume an HTML body. Found while fixing Panda's "Something went wrong reaching Panda" browser report (2026-08-20): a lapsed session's stale CSRF token made this hook abort *before* `panda/routes.py`'s own auth check ever ran, and `static/js/panda.js`'s `jsonFetch()` calling `response.json()` unconditionally threw on the HTML body. Panda's frontend was fixed to treat any non-JSON body as a failed result (see `docs/CHANGELOG.md`'s 2026-08-20 entry), but the underlying inconsistency — this hook doesn't distinguish a JSON API caller (`fetch()`) from a real page navigation (`<form>` POST) — remains for every other endpoint in the app | Low (every other POST endpoint today is a real `<form>` submission that already expects an HTML response — Panda is currently the only JSON `fetch()`-based POST caller in the app, so no other endpoint is actually affected in practice) | `app.py` (`enforce_request_security`) | 2026-08-20 (found while investigating Panda's browser-reported session-lapse defect) | Open — closing this would need the hook to check something like an `X-Requested-With`/`Accept: application/json` header (or a shared "is this blueprint JSON-only" registry) and return `jsonify({"error": "csrf_invalid"}), 400` for those callers instead of the HTML error template; out of scope for the Panda-only fix that found it |
| TD-65 | `panda/intents.py`'s new `_reply_retention()` (Panda's retention/cash topic expansion, ADR-52) composes `get_top_risk_students()`'s per-student `reasons` into a list, but there is still no function anywhere that aggregates *why* across the whole at-risk roster (e.g. "12 of 15 at-risk students are at risk because of late payment") — `compute_student_risk()`'s reasons are strictly per-student, and no cross-student tally exists. A library whose real root cause is overwhelmingly one signal (e.g. almost every at-risk student is late on payment, not close to renewal) gets a real top-3 list, not a summarized root cause | Low (inherent to what data already exists, not a bug — same shape as TD-61: the reply never claims an aggregate cause it doesn't have, it just has a narrower per-student view than a true root-cause summary would need) | `panda/intents.py` (`_reply_retention`), `panda/insights.py` (`get_top_risk_students`) | 2026-08-20 (Panda retention/cash topic expansion, ADR-52) | Open — closing this needs a new aggregation function (tally `compute_student_risk()`'s `reasons` categories across every scored student, not just the top-3 High-risk ones) — new analysis logic, not reuse, the same line ADR-49/TD-61 already drew for revenue's category breakdown |
| TD-68 | `utils/charts.py`'s `generate_membership_chart()` crashes with an unhandled `ValueError: Given lines do not intersect...` (raised deep inside `matplotlib.bezier.get_intersection()`, via `ax.annotate()`'s leader-line `connectionstyle="angle"` computation, via `fig.tight_layout()`) whenever the pie chart ends up with exactly two plan categories at an exactly equal (50/50) split — the two wedge-label leader lines land at exactly opposite angles, which is a degenerate case matplotlib's own "angle" connection style cannot resolve. Found live, not theoretical: `tests/test_08_cross_tenant_isolation.py::test_admin_b_cannot_view_admin_a_student_risk_via_ai_center` failed deterministically (3/3 reruns) during a full-suite regression pass for unrelated work (TD-30/TD-43/ADR-53) — root-caused to `tests/test_08_cross_tenant_isolation.py`'s `_register_and_login()` helper reusing a **hardcoded, non-randomized** username (`qa_tenant_iso_ai_a`) across every run of the whole test suite ever, rather than a fresh one per run (unlike every other fixture in this codebase, which suffixes with `_rand()`) - combined with `tests/conftest.py` having no teardown (see that file's own known limitation), that one admin has accumulated real membership rows since 2026-07-26 across many prior sessions, and this session's run happened to be the one where the accumulated MONTHLY/CUSTOM plan counts landed on an exact 6/6 split, triggering the crash for the first time. **Not caused by, or related to, this session's TD-30/TD-43/waitress/provisioning work** — `utils/charts.py`, `routes/dashboard.py`, and `tests/test_08_cross_tenant_isolation.py` were not modified by that work (confirmed via `git diff`); this is a pre-existing latent bug that was always going to eventually trigger given the no-teardown test architecture, and this session's full-suite run happened to be the one that landed on the exact data shape that hits it | Medium (a real, reproducible 500 for any admin whose membership data happens to reach an exactly-symmetric category split — not hypothetical, confirmed live; separately, `tests/test_08_cross_tenant_isolation.py`'s hardcoded, un-randomized `"iso_ai_a"`/etc. suffixes mean that test's own admin accumulates unbounded data forever across every future run, the same unbounded-growth risk as the shared dev/test Supabase project generally) | `utils/charts.py` (`generate_membership_chart`, `generate_membership_distribution_donut`), `tests/test_08_cross_tenant_isolation.py` (`_register_and_login`'s hardcoded suffixes) | 2026-08-21 (found during full-suite regression verification for ADR-53, out of that work's scope to fix) | **Chart-crash half Resolved — 2026-08-28** (test-fixture-growth half still Open). The chart crash recurred live on the first real Render deployment (Pilot A, `rkbunnkkeiwrnvexihap`) the moment the first admin's membership data reached a split with a wedge centroid on the horizontal axis — 500'd `/dashboard` outright (the exact `matplotlib/bezier.py` `get_intersection()` `ValueError` from this row, in Render's logs). Fixed by routing both `generate_membership_chart()` and `generate_membership_distribution_donut()`'s leader-line `connectionstyle` through a new `_leader_line_connectionstyle(dx, dy)` helper that nudges `angleB` 1 deg off any multiple of 180 (`m = angle_b % 180; if m < 1 or m > 179: angle_b += 1`) so matplotlib's `angle` connection style never gets two parallel rays. Verified against the exact 50/50 two-plan split, a three-plan split whose largest plan is exactly 50%, and single-plan / multi-plan / zero-plan datasets. See the 2026-08-28 CHANGELOG entry. **Still Open:** `tests/test_08_cross_tenant_isolation.py`'s `_register_and_login()` still uses hardcoded, un-randomized username suffixes, so that test's admin keeps accumulating membership rows in the shared dev/test project on every run — randomize those suffixes the way every other fixture already does. |
| TD-67 | `requirements.txt` has an undocumented minimum Python version — `contourpy==1.3.3` (a transitive `matplotlib` dependency, used by `utils/charts.py`) requires Python ≥3.11, but neither `docs/DEPLOYMENT.md` nor any other doc stated a Python version requirement before this was found. Verified live during the one-deployment-per-library provisioning work: a clean `pip install -r requirements.txt` into a fresh Python 3.10 virtualenv fails outright with "No matching distribution found for contourpy==1.3.3"; the identical install succeeds cleanly on Python 3.11 | Low (fails loudly and immediately at install time, not a silent runtime bug — but would have blocked Step C of a new library's provisioning with a confusing error if not documented) | `requirements.txt`, `docs/DEPLOYMENT.md`, `docs/PROVISIONING.md` | 2026-08-21 (found while verifying a clean-from-scratch install for the pilot provisioning work) | **Resolved** — 2026-08-21: `docs/DEPLOYMENT.md` step 1 and `docs/PROVISIONING.md`'s Prerequisites now both state Python 3.11+ explicitly. **Moot as of 2026-08-28 (ADR-56):** `matplotlib`/`contourpy` are removed from `requirements.txt` entirely (charts moved to client-side Chart.js), so no dependency imposes a minimum Python version any more. `.python-version` was changed the same day from `3.11.9` to a bare `3.12` — Vercel's `@vercel/python` builder has no `3.11.9` interpreter and `uv sync --locked` fails hard ("No interpreter found for Python 3.11.9") on an exact patch pin. |
| TD-66 | `payments.idempotency_key`/`payments.cashbook_synced`/`memberships.idempotency_key` (TD-30/TD-43's fix, ADR-53) are documented in `database/supabase_migration.sql` but this app cannot apply that `ALTER TABLE` to a live Supabase project itself (same constraint as TD-50/TD-53/TD-55/TD-58 — no `exec_sql`/DDL RPC, no direct-Postgres path, ADR-14). Unlike TD-58's discount columns, these silently degrade rather than fail: `database/membership_queries.py`'s `insert_membership()` and `database/payment_queries.py`'s `record_payment()` detect the missing column(s) and retry without them, so double-submit protection and cashbook-sync flagging simply aren't active on an un-migrated project — confirmed live against this repo's own dev/test Supabase project, which does not yet have any of the three columns; the new tests written for TD-30/TD-43 (`tests/test_03_student_membership_payment.py`) detect this and `pytest.skip()` themselves rather than fail | Medium (the two fixes this session was specifically about aren't actually protecting anything until this manual step runs — same severity class as the bugs it's meant to close) | `database/supabase_migration.sql`, `database/membership_queries.py`, `database/payment_queries.py` | 2026-08-21 (TD-30/TD-43 fix, ADR-53) | Open — apply the three documented `ALTER TABLE` statements in the Supabase SQL Editor, same manual step TD-50/TD-53/TD-55/TD-58 needed. `docs/PROVISIONING.md`'s Step A folds this into fresh-project setup automatically going forward; only already-provisioned projects (including this repo's current dev/test project) need it applied separately. |
| TD-70 | `database/panda_queries.py`'s five functions each wrapped their Supabase call in `except APIError` only — a transient network/connection failure (`httpx.TransportError`, e.g. `httpx.RemoteProtocolError` "Server disconnected") is not an `APIError`, so it fell straight through as an unhandled exception, surfacing to the admin as a raw 500 HTML error page instead of a clean JSON error. Caught live, not theoretical: reproduced unprompted during the 2026-08-24 business-admin audit, crashing `add_message()` mid-conversation. Compounded by `static/js/panda.js`'s `isSessionLapse()`, which treated *any* unparseable response (including this raw HTML 500 page) as session-lapse evidence, so the admin saw "Your session has expired" for a problem that had nothing to do with their login | Medium (a real, live-observed crash + a misleading error message on top of it, though intermittent — depends on a transient Supabase-side network blip actually occurring) | `database/panda_queries.py`, `static/js/panda.js` | 2026-08-24 (found live during the business-admin coverage/truthfulness audit) | **Resolved** — 2026-08-24 (ADR-54): `database/panda_queries.py` now also catches `httpx.TransportError`, raising a new, distinct `ChatStorageTemporarilyUnavailable` (separate from `ChatStorageUnavailable`, which stays scoped to "table doesn't exist"); `panda/routes.py` maps it to its own honest 503 body. `static/js/panda.js`'s `isSessionLapse()` now only fires on real session evidence (a 401, or the CSRF guard's specific 400+non-JSON combination) — a new `isBackendFailure()`/`SERVICE_UNAVAILABLE_MESSAGE` pair covers everything else. See `tests/test_14_panda_reliability_and_coverage.py` for the reproduction + regression tests and the 2026-08-24 CHANGELOG entry. |
| TD-74 | `app.py`'s `create_app()` did not trust any reverse-proxy headers (`X-Forwarded-Proto`/`X-Forwarded-For`) — no `ProxyFix` or equivalent was applied anywhere. Render, like effectively every PaaS host, terminates TLS at an edge proxy and forwards requests to the app process over plain HTTP internally, setting those headers. Without trusting them: (a) `request.is_secure` is always `False` in that deployment shape, so `set_security_headers()`'s `Strict-Transport-Security` (HSTS) header — guarded by `if request.is_secure:` — would never actually be set, even though the real client connection is genuinely HTTPS; (b) `utils/security.py`'s `rate_limited()` (applied to `routes/auth.py`'s `/` login and `/forgot-password`) keys its per-client attempt counter off `request.remote_addr`, which behind an untrusted proxy shows the proxy's own internal IP for every visitor — collapsing what's meant to be a per-client login-attempt limit into one shared bucket for the whole deployment (one person's failed attempts would count toward, and could exhaust, the same limit as anyone else hitting that deployment's login page). Found during the pre-Render-deployment-readiness audit while checking "Production HTTPS/session configuration" | High while undiscovered (silently defeats a real security header and turns a per-client rate limit into a shared one, on the exact platform shape — behind a TLS-terminating reverse proxy — this app was being prepared to run on), trivial once found — Werkzeug ships `ProxyFix` built in, no new dependency | `app.py` (`create_app`) | 2026-08-25 (pre-Render-deployment-readiness audit, Pilot A) | **Resolved** — 2026-08-25: `create_app()` now wraps `app.wsgi_app` in `werkzeug.middleware.proxy_fix.ProxyFix(x_for=1, x_proto=1)` (trusting exactly one hop, matching Render's single edge-proxy topology) before any request is served. Verified live: `app.wsgi_app` is a `ProxyFix` instance after `create_app()`; the full `test_00_smoke.py`/`test_01_auth.py` suite (42 tests, including the rate-limiter's own tests) still passes unchanged — Flask's test client sends no `X-Forwarded-*` headers, so `ProxyFix` is a no-op for every existing test, exactly as intended for local/test environments with no real proxy in front. See this session's CHANGELOG entry. |
| TD-73 | `routes/setting.py`'s `_save_upload()` (Library Profile logo/stamp/signature) and `backup_create()` (Settings → Data & Backup → "Create Backup") both write to the local filesystem — `static/uploads/settings/` and `<app root>/backups/` respectively — with no fallback to Supabase Storage or any other durable store. On a host with an ephemeral filesystem for its web service and no persistent disk attached (the default for a Render web service, and most PaaS free/starter tiers), every uploaded logo/stamp/signature is silently lost on the next restart or redeploy — the database row referencing it (`library_settings`) survives, but the file it points to does not, so the image would 404 until re-uploaded. Found during the pre-Render-deployment-readiness audit while checking for local-filesystem assumptions that won't work reliably on Render (session also fixed a related, more urgent logging gap — see **TD-72**) | Medium (silent, delayed data loss — not visible until the next restart/redeploy, at which point previously-working branding images break with no error and no obvious cause pointing back at "the filesystem isn't persistent here") | `routes/setting.py` (`_save_upload`, `backup_create`) | 2026-08-25 (pre-Render-deployment-readiness audit, Pilot A) | **Resolved — 2026-08-28.** Backup half (ADR-56): `backup_create()` builds the export with `json.dumps(...).encode()` and streams `send_file(io.BytesIO(...))` — no `os.makedirs`, no local file. Uploads half (ADR-57): `_save_upload()` now uploads logo/stamp/signature to a public Supabase Storage bucket (`library-branding`, auto-created on first use) via `database/branding_storage.py` and stores the full public URL in `library_settings`; `remove_library_logo()` deletes the Storage object; `utils/branding.py`'s `branding_src()` (a Jinja global) still resolves any legacy `uploads/settings/…` row. Works identically on Render and the Vercel deployment; `static/uploads/settings/` is now legacy-read-only. See the 2026-08-28 CHANGELOG entries. |
| TD-72 | `app.py`'s `_configure_logging()` attached only a `RotatingFileHandler` to `app.logger` — verified live (this session) that Python's `logging` module has no automatic console/stderr fallback once *any* handler is attached to a logger (the root logger's `lastResort` handler only fires when literally zero handlers exist anywhere in the hierarchy), so every `app.logger` call, including `_log_connected_supabase_project()`'s "Connected Supabase project: ..." startup line — which `docs/DEPLOYMENT.md` (step 5) and `docs/PROVISIONING.md` (Step D) both require an operator to read and visually confirm before declaring go-live, part of the one-deployment-per-library isolation model's own safety check (ADR-53) — was being written only to a local file. Render (like most PaaS hosts) only captures stdout/stderr as "Logs" for a web service and gives it no readable/persistent local filesystem by default, so this documented, safety-critical verification step would have been silently impossible to perform on the very platform this app was being prepared for | High while undiscovered (a required safety check with no way to actually perform it, for a model — one-deployment-per-library — whose entire cross-tenant-isolation guarantee (ADR-53) depends on an operator actually being able to perform it), trivial once found | `app.py` (`_configure_logging`) | 2026-08-25 (pre-Render-deployment-readiness audit, Pilot A) | **Resolved** — 2026-08-25: `_configure_logging()` now also attaches a `logging.StreamHandler` (stdout), unconditionally, alongside the unchanged `RotatingFileHandler` — verified live that `app.logger.handlers` now includes both and that the "Connected Supabase project: ..." line prints to stderr. See this session's CHANGELOG entry. |
| TD-71 | `database/cashbook_queries.py`'s `get_cash_balance()` (Cash-payment-method income minus Cash-payment-method expense) is all-time only — there is no function anywhere in this codebase that computes a *monthly* Cash-payment-method balance, the same gap TD-61 already documents for expense categories. Surfaced while wiring `panda/insights.py`'s `get_cash_balance()` wrapper and `panda/intents.py`'s new `INTENT_CASH_BALANCE` (ADR-55, 2026-08-25): a question like "what was my cash balance last month?" has no real data to answer from, and `_reply_cash_balance()` deliberately only ever states the all-time figure rather than fabricating a monthly one | Low (inherent to what data already exists, not a bug — `_reply_cash_balance()` never claims a monthly figure it doesn't have, the same "a check that finds nothing contributes nothing" discipline TD-61/TD-65 already follow) | `database/cashbook_queries.py` (`get_cash_balance`), `panda/insights.py` (`get_cash_balance`), `panda/intents.py` (`_reply_cash_balance`) | 2026-08-25 (pre-pilot cash-balance/profit-loss coverage pass, ADR-55) | Open — closing this needs a new `_monthly_totals_by_type()`-style aggregation scoped to `payment_method == "Cash"` (mirroring `get_monthly_income()`/`get_monthly_expense()`'s existing shape, which `get_monthly_profit()` already composes) — new aggregation logic, not a wrapper |
| TD-75 | `requirements.txt` was saved as UTF-16 LE (with a `FF FE` BOM, one `\x00` byte after every character) rather than UTF-8. `pip install -r requirements.txt` from a Windows PowerShell shell reads it without complaint, so every local venv and the 2026-08-25 readiness audit missed it — but `pip` on a Linux build image (Render, and any other non-Windows CI/host) cannot parse a UTF-16 requirements file and the build fails before any application code runs. Found while preparing `render.yaml` for the first real Render deployment | High while undiscovered (a hard build-time failure on the exact target platform, invisible from local Windows development), trivial once found — re-encode the file, no dependency or version change | `requirements.txt` | 2026-08-27 (Render Blueprint preparation) | **Resolved** — 2026-08-27: re-saved byte-for-byte (same 55 pinned packages, no version changed) as UTF-8 without BOM. `docs/DEPLOYMENT.md` and `docs/FILE_REFERENCE.md`'s new `requirements.txt` card both now state the UTF-8 requirement and give the `pip freeze | Out-File -Encoding utf8` form for regenerating it safely from PowerShell. See this session's CHANGELOG entry. |
| TD-69 | A brand-new Supabase project does not automatically grant the service-role-equivalent API key any privileges on tables created by hand-running `database/supabase_migration.sql` in the SQL Editor — every table read fails with Postgres error `42501 permission denied for table X` until `GRANT ALL ON ALL TABLES/SEQUENCES IN SCHEMA public TO service_role` (plus matching `ALTER DEFAULT PRIVILEGES`) is run as a separate step. `docs/PROVISIONING.md`'s Step A previously assumed a fresh Supabase project auto-configures these grants the way it does for tables created via the dashboard's Table Editor — that assumption held for this repo's original dev/test project but did not hold for a genuinely fresh project (`smart-library-pilot-asti`) created and migrated during the 2026-08-24 clean-deployment pilot validation. `scripts/verify_schema.py` reported every table as `ERROR` (not the more diagnostic `MISSING`) when this happened, so the actual cause (a Postgres GRANT gap, not a missing table) wasn't obvious from the script's own output — required fetching the raw PostgREST response directly to see the real `42501`/hint | Medium (would fully block go-live on any newly provisioned project until diagnosed — not hypothetical, reproduced live on the first fresh project created after `docs/PROVISIONING.md` existed) | `database/supabase_migration.sql`, `docs/PROVISIONING.md`, `scripts/verify_schema.py` | 2026-08-24 (fresh-Supabase-project pilot provisioning validation, `smart-library-pilot-asti`) | **Resolved** — 2026-08-24: `docs/PROVISIONING.md` Step A now includes the required `GRANT`/`ALTER DEFAULT PRIVILEGES` statements as an explicit sub-step run immediately after the migration script, before `scripts/verify_schema.py`. The grants are documented as a manual runbook step, not folded into `database/supabase_migration.sql` itself — this codebase has no way to verify a SQL file change against a live project without a human running it either way (ADR-14), so keeping it as its own clearly-labeled sub-step (rather than silently appending it to the existing migration block) was judged clearer for an operator debugging a `42501` after already running the migration once. |
| TD-76 | The Dashboard and Membership Distribution pages now load `Chart.js 4.4.4` from the jsdelivr CDN (`<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.4/...">`) at runtime to draw their charts client-side (ADR-56, 2026-08-28) — the same CDN dependency the Business Intelligence and Cashbook pages have had since they were built. A deployment behind a strict Content-Security-Policy that doesn't allow `cdn.jsdelivr.net`, or one that must work fully offline, would get blank chart areas on all four page groups | Low (consistent with the pre-existing BI/Cashbook behavior, not a regression against how those already worked; the app's other CDN deps — Bootstrap, Bootstrap Icons — have the same property) | `templates/dashboard/index.html`, `templates/memberships/distribution.html` (and pre-existing: `templates/business_intelligence/*.html`, `templates/cashbook/index.html`) | 2026-08-28 (chart move to Chart.js, ADR-56) | Open — vendor `chart.js@4.4.4/dist/chart.umd.min.js` into `static/js/` and point all chart-page `<script>` tags at `url_for('static', ...)` if a strict-CSP or offline deployment is ever needed; do it for all four page groups at once, not just the two changed here |
| TD-77 | `config.py`'s `MAX_CONTENT_LENGTH = 5 * 1024 * 1024` (5 MB) is larger than Vercel's serverless request-body cap (~4.5 MB). On the Vercel deployment, an upload between ~4.5 MB and 5 MB is rejected by Vercel's edge with its own 413 before the request ever reaches Flask, so the app's own 413 handler / "file too large" message never runs and the limit the UI implies (5 MB) isn't the real one there. Render is unaffected (no such platform cap) | Low (only affects uploads in a narrow 0.5 MB size band, only on Vercel, and the outcome is still "upload rejected" — just with a less friendly error than the app's own) | `config.py` (`MAX_CONTENT_LENGTH`), Vercel platform limit | 2026-08-28 (Vercel Hobby deploy, ADR-56) | Open — if it matters, lower `MAX_CONTENT_LENGTH` to ~4 MB (affects both hosts) or special-case it by env; not worth doing for a testing deployment |
| TD-78 | `routes/enquiries.py`'s `add()` and `routes/student.py`'s `admission()` assigned `enquiry_id` / `student_id` by reading `MAX(id)` and inserting `MAX+1` as an explicit value (the identity sequence trails ordinary usage — ADR-15/18/29/30). That read-then-insert races: a double-click / back-button resubmit sends two POSTs that both read the same `MAX` and both try to insert it; the loser hit a primary-key collision (`23505`) that surfaced as a bare "Something went wrong. Please try again." — and, separately, `add()` had no duplicate detection at all, so a genuine double-submit that *didn't* collide produced two identical enquiry rows (the "one Admitted, one Interested for the same person" pair a pilot tester reported). Payments were hardened against the same class of bug (idempotency key, TD-30/ADR-53) but enquiry-add and admission never were | Medium (user-visible: a confusing generic error on one path, silent duplicate rows on the other — both on the very first workflow step a new library uses) | `routes/enquiries.py` (`add`), `routes/student.py` (`admission`) | 2026-08-28 (pilot testing on the Vercel deployment) | **Resolved** — 2026-08-28. (1) New `database/id_sequence.py` `insert_with_next_id(table, id_column, row)` does the `MAX+1`/insert with a retry loop on `23505` (up to 6×), so a concurrent insert just takes the next id instead of erroring; both `add()` and `admission()` use it. (2) The duplicate-person half was fixed by the **ADR-58** redesign, not a 2-minute window: `add()` now treats `(admin_id, mobile)` as a person's identity — if the number is already on file it creates nothing and redirects to that person's `enquiry.view` / `student.view` record; a repeat visit uses the new `re_enquire()` route ("Log Another Enquiry"), which updates the person's single enquiry row in place and re-opens it. `edit()` rejects a mobile change onto another person's number. App-level enforcement, no schema change. See ADR-58 and the 2026-08-28 CHANGELOG entry. |

## Stub Routes & Planned Features

Not defects — intentionally incomplete, called out so nobody mistakes "coming soon" for a bug.

| ID | What | Where |
|---|---|---|
| PF-1 | **Resolved** — Receipt Settings half resolved 2026-07-20; Notification Settings half resolved 2026-07-21 (full GET/POST handler, template, and query module now exist). See both entries in [CHANGELOG.md](CHANGELOG.md). | `routes/setting.py`'s `notification_settings()` |
| PF-2 | **Resolved (as a redirect, not a build-out)** — Membership Analytics previously requested a nonexistent template path (`membership/analytics.html`) and crashed on every visit; the template it presumably meant to reach, `templates/memberships/analytics.html`, turned out to be a pre-existing 0-byte file, so a path fix alone would only have produced a blank, chrome-less page. 2026-07-22 (QA & Validation Sprint): now permanently redirects to the fully-implemented Membership Distribution page instead, mirroring PF-3's pattern. See [CHANGELOG.md](CHANGELOG.md). | `routes/membership_analytics.py` |
| PF-3 | Reports permanently redirects to Business Intelligence — kept only as a URL-compatibility shim | `routes/report.py` |
| PF-4 | Staff & User Access is a professional "Coming Soon" placeholder only — still single-admin, no multi-user auth, no role-based permissions | `routes/setting.py`'s `staff_access()`, `templates/settings/staff_access.html` |
| PF-5 | Data & Backup only supports manual Export CSV / Create Backup — automatic/scheduled backups are not implemented | `routes/setting.py`'s `data_backup()`/`backup_create()` |
| PF-6 | **Resolved** — 2026-07-25: Revenue Analytics and Occupancy Analytics (added alongside Purpose Analytics) were UI/navigation shells only, with every value a hardcoded placeholder (see TD-46). All three now render real, admin-scoped data: Purpose Analytics since 2026-07-26 (Phase 1), Revenue Analytics since 2026-07-26 (Phase 2, ADR-37), Occupancy Analytics since 2026-07-26 (Phase 3, ADR-38, which also added rule-based AI insights — `database/bi_queries.py`'s `get_occupancy_insights()`). No filters, export, or drill-down exist on any of the three yet — not part of any of these three phases' scope | `routes/business_intelligence.py` (`revenue_analytics`, `occupancy_analytics`), `templates/business_intelligence/{revenue,occupancy}_analytics.html` |
| PF-7 | AI Center (`routes/ai_center.py`) ships Student Risk Analysis (Phase 1) and Settings (Phase 3, pulled forward — ADR-40) — an admin can look up a student's risk breakdown and edit the scoring weights/thresholds that produce it. Still not built: **Phase 2**, an aggregate Prediction Dashboard (portfolio-level risk counts/distribution/trend, reusing `compute_student_risk()` per student — a third tab alongside the two that exist now). As of 2026-08-18 (ADR-48), `panda/insights.py`'s `get_top_risk_students()` does loop `compute_student_risk()` over an admin's roster for Panda's own chat replies — **this is not Phase 2** (no dashboard/UI, capped at 100 students for chat-latency reasons, see TD-60, High-risk-only, no distribution/trend view), but it's the first piece of aggregate-risk orchestration in the codebase and could inform Phase 2's design if it's ever built. The separate AI Chat Assistant this row used to describe as "planned, no backend chosen" now exists as its own module — see **PF-8** — deliberately **not** built as a third AI Center tab; it's a different subsystem (a global floating widget, not a page within this one) | `routes/ai_center.py`, `database/ai_center_queries.py`, `database/ai_center_settings_queries.py` |
| PF-8 | **Resolved for the rule-based scope this app can support — the Ask→Analyze→Explain→Recommend→Forecast progression is fully built** (2026-08-19, ADR-47/ADR-48/ADR-49/ADR-50/ADR-51 — see `PANDA_SPEC.md`). Panda (`panda/` package, ADR-43) ships the complete architecture: a floating button + slide-out chat panel on every authenticated page, "Today's AI Insights" (three rule-based checks, computed live, no new table), Suggested Questions, and a real multi-conversation chat history store (`panda_conversations`/`panda_messages` — see TD-55 for the one manual setup step). All five parts are built: `panda/intents.py` is a real rule/keyword-based intent classifier — revenue, occupancy, renewals, admissions trend, purpose/course performance (including "which batch" questions, answered via purpose data with an explicit disclaimer), student risk (real top-N High-risk list, reusing `compute_student_risk()`), a "why is revenue X" explanation (`panda/insights.py`'s `get_revenue_explanation()`, ranked real factors — new-admissions momentum, unrenewed expirations, pending collections), a "how can I increase profit?"/"what should I do?" reply (`get_recommended_actions()`, a pure wrapper around `database/bi_queries.py`'s existing `get_action_items()` Action Center, no new recommendation logic), and now a "what's expected next month?" reply (`panda/forecasting.py`, a plain-Python linear trend extrapolation over real monthly revenue/occupancy/lapse-rate history, honestly returning "not enough history yet" rather than a number below `MIN_MONTHS_FOR_FORECAST` — see ADR-51) all get a real, data-backed reply; action requests (deletion/pricing) are declined and redirected; "which locality gives the highest admissions" stays unanswered on purpose — no locality data exists anywhere in this schema. Still not built, and not part of this progression: a real language model or ML — no `scikit-learn`/`statsmodels`/`prophet`/LLM-API dependency exists anywhere in this codebase, and none of Panda's five parts required one; that remains a distinct, unstarted future capability, not a gap in what was scoped here. AI Center's aggregate Prediction Dashboard (PF-7's Phase 2) also remains unbuilt (Part 2's risk-list orchestration is Panda-scoped and capped, not that dashboard — see TD-60). The read-only/no-SQL/no-secrets security boundary (`panda/prompts.py`'s `READ_ONLY_RULES`) is enforced today only because no tool-calling code path exists at all yet — every `panda/intents.py`/`panda/insights.py`/`panda/forecasting.py` function only ever calls existing read-only query functions, so this remains true after ADR-47/48/49/50/51, but it's still documentation for a future LLM/tool-calling layer, not a runtime filter, and must be re-verified as an actual enforcement mechanism once one is added. See **TD-59** for `detect_intent()`'s keyword-coverage limitation, **TD-60** for the risk-scoring roster cap, **TD-61** for `get_revenue_explanation()`'s lack of a category-level factor, and **TD-62** for the forecasting method's inherent simple-linear-fit limitations (no seasonality, small-sample sensitivity). As of 2026-08-20 (ADR-52), two more topic-specific intents — `INTENT_RETENTION` ("how can I retain students?", "how do I reduce churn?") and `INTENT_CASH` ("how can I manage cash?", "where is my cash going?") — close instances of TD-59's keyword-coverage gap by composing existing `panda/insights.py` wrappers into topic-focused replies; this is a lateral expansion of Part 1's keyword coverage, **not a sixth part of the progression**, which remains exactly as scoped above. See **TD-60** (inherited, and now surfaced honestly when its cap is hit) and **TD-65** (retention's per-student, not cross-student-aggregate, "why" reasons) for this addition's own known limitations | `panda/routes.py`, `panda/services.py`, `panda/prompts.py`, `panda/intents.py`, `panda/insights.py`, `panda/forecasting.py`, `database/panda_queries.py` |

## Next logical slices (recommendations, not commitments)

1. ~~**Fix TD-32 first** — `/settings/backup/create` downloads the entire multi-tenant database (all admins' data + password hashes), not just the requesting admin's.~~ — **done 2026-07-25** (ADR-32, forced by Phase 11's SQLite removal), see TD-32's `Resolved` status and the changelog entry.
2. ~~Fix TD-8 (`requirements.txt`)~~ — verified already fixed (2026-07-22); doc was stale.
3. ~~Add a first test around `get_business_health_score`/Cashbook aggregates (TD-20)~~ — **done 2026-07-22**, see the QA & Validation Sprint changelog entry; this also surfaced TD-33 (health-score docstring/behavior mismatch for zero-activity libraries).
4. ~~Wire the new receipt settings (TD-22) into `routes/membership.py`/`routes/payment.py`'s receipt generation~~ — **done 2026-07-22**, see ADR-13. Next slice in this area: TD-30's double-submit protection, which would need an idempotency token/key on the payment forms — a real feature addition, not a quick fix.
5. Build an actual reminder/notification dispatch job (TD-24) that reads `library_settings`'s `reminder_*`/`notify_*`/`quiet_hours_*` columns and sends something — currently every channel toggle and quiet-hours setting is a no-op preference. The same job (or `routes/notification.py` directly) is also where TD-28's rule toggles should start being honored for the in-app bell/Notifications page, before worrying about external channels.
6. If session-timeout/login-notification enforcement (TD-26) is ever prioritized, this is also the natural point to introduce the centralized auth-check hook recommended for TD-19, since both need a `before_request`-style entry point.
7. The remaining half of TD-7 (`late_fee_per_day`, `renewal_grace_days`, `auto_expiry`, `allow_early_renewal`) needs actual UI/automation surface before it can be wired in — e.g. a late-fee line item on Renew, a grace-window check before allowing renewal, and a scheduled job for auto-expiry — each is a real feature addition, not a quick wire-up like the plan pricing half was.
8. ~~Close TD-35: migrate `routes/setting.py`'s `security_settings()` password-change branch to `database/supabase_client.py`'s Supabase client~~ — **done 2026-07-23**, see the changelog entry and TD-35's `Resolved` status. Next slice: migrating the rest of `routes/setting.py` (`library_settings`/`membership_settings`/`backup_log`) plus `routes/enquiries.py` (`enquiries`), `routes/student.py` (`students`), and the Cashbook/Membership/Payment trio that writes `audit_log` would additionally let `register()`'s SQLite mirror-insert bridge (added 2026-07-23 to satisfy all 7 of those tables' SQLite foreign keys to `admins.admin_id`) be deleted — but only once *all seven* no longer need it; this session's password-branch migration alone doesn't touch any of those 7 tables (it only changed `admins.password`'s writer), so the mirror-insert bridge is still fully required.
9. ~~Migrate `routes/enquiries.py`'s `enquiries` table to Supabase~~ — **done 2026-07-23** (ADR-18), see the changelog entry. This closed 0 of `register()`'s remaining 6 FK dependencies by itself, since `enquiries` needed its own `add()`/`edit()`/`delete()` mirror rather than a one-shot bridge (see ADR-18's "Why") — the SQLite mirror is still required, and its status column had a new, separate split-brain risk (TD-36).
10. ~~Migrate `routes/student.py`'s `students` table to Supabase~~ — **done 2026-07-23** (ADR-19), see the changelog entry. This closed TD-36 at the source (`admission()`'s `enquiries.status` write now targets Supabase directly) but, like `enquiries`, closed 0 of `register()`'s remaining 6 FK dependencies — `students` also needed an ongoing `admission()`/`edit()` mirror, not a one-shot bridge, since 8 other unmigrated modules (`routes/membership.py`, `routes/payment.py`, `routes/dashboard.py`, `routes/membership_distribution.py`, `routes/notification.py`, `routes/setting.py`'s backup functions, `database/bi_queries.py`, `database/cashbook_queries.py`, `database/membership_queries.py`) all run raw `JOIN students` directly against SQLite. Both the `enquiries` and `students` SQLite mirrors remain required — verified post-migration that `routes/dashboard.py`'s `enquiries` count query and `routes/enquiries.py`'s own `students` lookup are untouched and still need them. Next slice: pick one of the 5 remaining SQLite-only tables (`audit_log`, `library_settings`, `membership_settings`, `backup_log`, `security_settings`) to close more of `register()`'s FK dependencies, or migrate Membership+Payment together (they already share `database/payment_queries.py`/`membership_queries.py`) to start shrinking the `students` SQLite mirror's footprint.
11. ~~Migrate `routes/membership.py`'s `memberships` table to Supabase~~ — **done 2026-07-23** (ADR-20), see the changelog entry. Payment was deliberately *not* included in this slice despite item 10's own "Membership+Payment together" suggestion, per this session's explicit scope. This closed 0 of `register()`'s remaining 6 FK dependencies (`memberships` was never one of the 7 — its FK to `admins` runs through `students`, not directly) and closed 1 of the `students` SQLite mirror's consumers (`routes/membership.py`'s own student lookups now go through Supabase instead), leaving `routes/payment.py`/`routes/dashboard.py`/`routes/membership_distribution.py`/`routes/notification.py`/`routes/setting.py`'s backup functions/`database/bi_queries.py`/`cashbook_queries.py` still needing it. It introduced its own new mirror (`memberships`, needed by `routes/payment.py`'s `collect()`, `routes/dashboard.py`, `routes/membership_distribution.py`, `routes/notification.py`) and its own new split-brain (TD-37, `paid_amount`/`pending_amount`, open at the time).
12. ~~Migrate `routes/payment.py`'s `collect()` to Supabase for `memberships.paid_amount`/`pending_amount`~~ — **done 2026-07-23** (ADR-21), see the changelog entry. Closed **TD-37** at the source (now `Resolved`) — `collect()` writes Supabase first, same copy `routes/membership.py`'s `index()` reads, then mirrors into SQLite. Closed 0 of `register()`'s remaining 6 FK dependencies (unrelated table). Shrank the `memberships` SQLite mirror's remaining-consumer list by one (`routes/payment.py`'s own balance update now goes through Supabase too), but that list turned out wider than ADR-20 had predicted: `routes/dashboard.py`, `routes/membership_distribution.py`, `routes/notification.py`, `routes/student.py`'s `view()`, `database/cashbook_queries.py`'s `get_pending_fees()`, and `database/bi_queries.py` all still need it — none were touched, so the mirror stays. `payments`/`cashbook`/`audit_log` remain entirely SQLite-only; `routes/payment.py`'s own `index()` is unchanged. Next slice: migrate one of the six remaining `memberships`-mirror consumers to shrink that mirror further, or pick one of the 5 remaining SQLite-only tables behind `register()`'s bridge (`audit_log`, `library_settings`, `membership_settings`, `backup_log`, `security_settings`) — both are open and independent of this slice.
13. ~~Migrate `routes/cashbook.py`/`database/cashbook_queries.py`/`database/audit_queries.py`'s `cashbook`/`audit_log` tables to Supabase~~ — **done 2026-07-23** (ADR-22), see the changelog entry. Closed 0 of `register()`'s 7 FK dependencies (`audit_log`'s SQLite mirror-write is unchanged, so the FK still fires) — but of those 7, the count still **fully unmigrated** drops from 5 to 4, since `audit_log` now joins `enquiries`/`students` as migrated-at-the-route-level while still requiring the bridge. Introduced **TD-38**/**TD-39** instead of a TD-35/36/37-shaped split-brain, because `payments` (the table `cashbook.payment_id` needs) is out of scope here and has a *live Postgres FK* Supabase enforces — a sharper constraint than any prior slice hit. Next slice: migrate `payments` itself (closes TD-38/TD-39 at the source, and is read directly by `routes/payment.py`'s own `index()`, `routes/membership_distribution.py`, `routes/student.py`'s `view()`, `utils/charts.py`, and `database/cashbook_queries.py`'s fee-revenue functions — a materially larger slice, since `record_payment()` is shared by `routes/membership.py` and `routes/payment.py`), or pick one of the 4 remaining fully-unmigrated tables behind `register()`'s bridge (`library_settings`, `membership_settings`, `backup_log`, `security_settings`) instead — independent of this slice.
